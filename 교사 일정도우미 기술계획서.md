# 교사 일정도우미 — 기술 계획서

쿨메신저 쪽지에서 학교 일정을 뽑아 **선생님이 아무것도 누르지 않아도** 캘린더에 넣고,
바탕화면 위젯으로 보여 주는 Windows 설치형 앱.

이 문서 하나로 처음부터 끝까지 만들 수 있도록 썼다. 특히 **8장 「반드시 지켜야 할 제약」**은
구현 중에 반드시 마주치는 함정을 미리 막는 부분이다. 시간이 없으면 8장부터 읽는다.

> **2026-08-27 결정:** 이번 구현은 R2(모바일 PWA·Firebase 서버 동기화)를 제외한 로컬 전용
> 범위로 확정한다. R0·R1·R3만 만들고, `mobile/`·`mobile-pwa/`·`firebase/` 디렉터리와 7.12·12A 관련
> 내용은 삭제하지 않되 향후 확장을 위한 참고 사양으로만 남긴다.

---

## 0. 이 문서와 PRD Release Gate의 관계

**이 문서는 제품 전체(R0~R3)를 다룬다. 그러나 실제 구현 순서는 PRD 0장의 지시를 따른다.**

> PRD 0장 3항: "R0만 먼저 구현하고 테스트한다. R0 승인 전에는 모바일 서버, 쿨메신저 UI
> 자동조작, 전국 배포를 구현하지 않는다."

이 문서 어디에도 R0/R1/R2/R3 표시가 없다고 해서 모든 기능을 한 번에 만들어도 된다는 뜻이
아니다. 아래가 이 문서의 기능과 PRD Release Gate의 대응표다. **표시가 없는 항목은 R0.**

| 기능 | Gate | 비고 |
|---|---|---|
| 파일 읽기·파이프라인·분류·후보함·캘린더·자동등록·Windows 로컬 알림 | **R0** | Claude Code 1차 구현 범위. 완료 후 사용자 승인 필요 |
| 바탕화면 위젯, 부팅 시 위젯 자동 실행, 위젯 즉시 반영 | **R1** | R0 승인 후 |
| 백업·복원 | **R1** | |
| 모바일 PWA, QR 기기 연결, 종단간 암호화 동기화, Web Push | **R2** | **이번 구현 범위 제외.** R1 승인 후. PRD의 R2 설계와 인수 기준을 따른다 |
| 쿨메신저 UI 자동화(수동+예약), 폴더 감시 자동 수집 | **R3** | R1 승인 후. PRD 4장이 UI 자동조작을 R0 제외 항목으로 명시 |

**2장의 시나리오 A~D는 제품 전체의 완성 기준이지, R0 하나의 완료 기준이 아니다.**
시나리오 A·B는 R3, C·D는 R1에 속한다. **R0 자체의 완료 기준은 PRD 20장 "R0 완료" 표를
따른다** (합성 XLS 가져오기, 후보 판정, 수동 등록·캘린더·로컬 알림, golden set 통과 —
UI 자동화나 위젯은 포함되지 않는다).

R2는 A~D와 별도의 모바일 완성 기준을 가진다. QR로 연결한 휴대폰에서 PC가 꺼져 있어도
암호화된 일정의 예약 알림을 받고, 기기 연결 해제와 서버 데이터 삭제가 동작해야 한다.

10장 구현 순서 표에도 이 경계를 다시 표시했다. **R0 구간이 끝나면 멈추고 승인을 기다린다.**

---

## 1. 문제와 사용자

**사용자**: 초·중·고등학교 모든 교사.

**하루에 벌어지는 일**: 쿨메신저로 교무부·학년부·보건실·영양실에서 쪽지가 쏟아진다.
그 안에 `8월 28일(금) 15시 교직원 회의`, `수요일까지 알레르기 조사표 제출` 같은 일정이
섞여 있다. 지금은 선생님이 눈으로 읽고 손으로 다이어리에 옮긴다. 놓치면 사고가 난다.

**기존에 이걸 해결하려면 선생님이 하는 일곱 단계**

1. 쿨메신저를 연다
2. 로그인한다
3. 메시지함을 연다
4. 기간을 정해 엑셀로 내려받는다
5. 저장 위치를 확인한다
6. 일정 앱을 연다
7. 파일을 골라 가져온 뒤 하나씩 옮겨 적는다

**이 앱의 목표는 이 일곱 단계를 0단계로 만드는 것이다.** 정해 둔 시각이 되면 앱이 알아서
1~5를 대신하고, 6~7은 아예 없어진다.

---

## 2. 완성 기준 — 이 네 가지가 되면 끝난 것이다

문서를 다 읽고 나서도 헷갈리면 이 절로 돌아온다. **아래 네 시나리오가 실제로 돌아가는 것이
완성이다.** 그 외는 부가 기능이다.

| # | 시나리오 | 확인 방법 |
|---|---|---|
| **A** | 앱 설정에서 단추 하나를 누르면, 이미 로그인됐거나 공급사 자동 로그인이 설정된 쿨메신저를 열어 메시지를 내려받고, 그 파일이 자동으로 캘린더에 반영된다 | 지원 쿨메신저 버전·기준 PC에서 쿨메신저를 완전히 종료한 상태로 단추를 누른다. 정상 경로의 목표는 10초 이내이며 단계별 타임아웃은 별도로 적용한다 |
| **B** | 정해 둔 시각(예: 매일 08:00)이 되면 A가 저절로 일어난다 | 시각을 1분 뒤로 맞추고 기다린다 |
| **C** | 컴퓨터를 껐다 켜면, 앱을 실행하지 않아도 바탕화면에 위젯이 떠 있다 | 실제로 재부팅한다. 로그인 후 앱 창은 뜨지 않고 위젯만 뜬다 |
| **D** | 일정을 저장·수정·삭제하면 위젯이 **즉시** 바뀐다 | 메인 창에서 일정을 지운다. 위젯에서 1초 안에 사라진다 |

**A·B는 R3, C·D는 R1에 속한다 (0장 대응표 참고). Claude Code가 지금 만드는 R0에는 네
시나리오 중 어느 것도 포함되지 않는다.** R0가 끝났다는 것은 사람이 수동으로 가져온 파일이
파이프라인을 통과해 후보함·캘린더·로컬 알림까지 정확히 동작한다는 뜻이다.

---

## 3. 범위

### 만든다

- 쿨메신저 내보내기 파일(.xls/.xlsx) 읽기 — **두 가지 서로 다른 내부 형식 모두** [R0]
- 쪽지에서 일정·마감 후보 추출 (규칙 기반) [R0]
- 최초 실행 시 역할 태그(담임·담당 학년·보직 등) 설정 화면 [R0]
- 조건을 갖춘 후보의 캘린더 자동 등록 [R0]
- Windows 예약 알림 [R0]
- 로컬 저장 [R0]
- 필드 단위 암호화 [R1] (PRD 15·19장 기준. R0는 평문 저장 후 R1에서 ciphertext로 전환)
- 바탕화면 위젯 (오늘·내일·임박 마감 / 달력 전환) [R1]
- 부팅 시 위젯 자동 실행 [R1]
- 모바일 PWA, QR 기기 연결, 종단간 암호화 동기화, Web Push [R2] (이번 구현 범위 제외)
- 폴더 감시 자동 수집 [R3]
- 쿨메신저 UI 자동 조작으로 내려받기 (수동 + 예약) [R3]

### 만들지 않는다

- R0·R1·R3의 로컬 기능이 서버·클라우드·계정에 의존하는 구조
- R2 서버로 원본 메시지나 일정 제목·장소·메모를 평문으로 전송하는 구조
- 쿨메신저 서버 프로토콜 직접 연동 (내보낸 파일만 읽는다)
- Windows 이외 OS (알림·키 보호 경로가 없다)
- LLM 호출 (규칙 기반으로 충분하고, 원문이 밖으로 나가면 안 된다)

---

## 4. 기술 스택

| 층 | 선택 | 이유 |
|---|---|---|
| 셸 | **Tauri 2** (Rust + WebView2) | 앱 본체를 작게 유지하고, 사용자 계정 설치와 Win32 API 호출을 지원. 설치본 크기는 WebView2 포함 방식에 따라 별도 측정 |
| 화면 | React 18 + TypeScript + Vite | |
| 저장소 | SQLite (`rusqlite`, bundled) | 파일 하나, 서버 없음 |
| 엑셀 | `calamine` + **자체 XML 파서** | calamine은 OLE2/ZIP만 읽는다. 8.1 참고 |
| XML | `quick-xml` | DTD·외부 엔티티를 해석하지 않는다 (XXE 안전) |
| 암호화 | `chacha20poly1305` + DPAPI + `argon2` | AES-NI 없는 저사양 학교 PC에서도 일정한 성능 |
| 날짜 | `chrono` + `chrono-tz` (Asia/Seoul) | |
| Win32 | `windows` 크레이트 | UI 자동화·알림·레지스트리 |

`windows` 크레이트에 필요한 feature (빠뜨리면 컴파일 안 됨):

```
"Data_Xml_Dom", "Foundation", "Foundation_Collections",
"UI_Notifications",
"Win32_Foundation", "Win32_Security_Cryptography",
"Win32_System_Com", "Win32_System_Com_StructuredStorage",
"Win32_System_Registry", "Win32_System_Threading", "Win32_System_Variant",
"Win32_UI_Accessibility",          ← UI 자동화. 빠뜨리기 쉽다
"Win32_UI_Shell", "Win32_UI_Shell_Common", "Win32_UI_Shell_PropertiesSystem",
"Win32_UI_WindowsAndMessaging"
```

---

## 5. 아키텍처

### 모듈 지도 (Rust)

```
src-tauri/src/
  lib.rs           앱 조립, 명령 등록, 시작 절차
  app.rs           AppState (DB 연결, 키, 취소 플래그)
  single_instance.rs  중복 실행 방지 (이름 있는 뮤텍스)

  adapters/
    mod.rs             FileDescriptor, 어댑터 트레이트, pick()
    coolmessenger_xls.rs  시그니처 판별 → 시트/열 매핑 → RawMessage
    spreadsheetml.rs      SpreadsheetML 2003 XML 읽기      ★ 8.1

  pipeline/
    mod.rs        전체 흐름 (인용분리 → 중복제거 → 마스킹 → 분류 → 날짜 → 후보)  ★ 7.2
    quote.rs      인용문 분리
    dedupe.rs     내용 지문·파일 지문 (마스킹 이전 원문 기준)
    security.rs   민감정보 탐지·마스킹
    segment.rs    문장 나누기
    datetime.rs   날짜·시각·교시 추출                      ★ 8.10
    classify.rs   분류와 신뢰도 점수
    candidate.rs  후보 생성, 제목 만들기, 자동등록 추천 판정
    relations.rs  변경·취소 신호 탐지

  autocollect.rs   폴더 감시 → 가져오기 → 자동 등록  [R3]  ★ 8.5
  messenger.rs     쿨메신저 UI 자동 조작 + 예약      [R3]  ★ 8.2~8.4
  widget.rs        바탕화면 위젯 창                  [R1]  ★ 8.7
  autostart.rs     부팅 시 자동 실행 (HKCU\...\Run)  [R1]  ★ 8.6
  notify/          Windows 예약 알림
  mobile/          R2 기기 연결·암호화 동기화·동기화 큐 [향후 확장 — 이번에는 생성하지 않음]
  db/              스키마·마이그레이션·저장소 접근
  crypto/          DPAPI 키 보호, 필드 암호화, 백업 암호화
  commands.rs      화면이 부르는 명령
  manage.rs        설정·위젯·자동수집·백업 명령
```

R2를 향후 재개하면 데스크톱 Rust 코드만으로 끝나지 않는다. 그때 같은 저장소에 다음 경계를
분리한다. **이번 구현에서는 아래 디렉터리를 생성하지 않는다.**

```text
mobile-pwa/        Service Worker, QR 연결, IndexedDB의 non-extractable key, 복호화 UI
firebase/functions/ pairing, 일정·알림 상태 API, 발송 직전 상태 버전 검증, Web Push
firebase/rules/     Firestore Security Rules와 App Check 정책
```

### 데이터 흐름 (한 줄로)

R0에서는 왼쪽 두 화살표(UI 자동화·폴더 감시, 둘 다 R3) 없이 **사용자가 파일 선택 대화상자로
직접 고른 파일**이 바로 어댑터로 들어간다.

```
[R3] 쿨메신저 창  ──UI 자동화──▶  coolmsg_*.xls  ──[R3]폴더 감시──▶  어댑터  ──▶  파이프라인
                                                                              │
                                              ┌───────────────────────────────┘
                                              ▼
                                     후보 (모호·민감·과거는 여기서 멈춤)
                                              │
                                     조건 통과분만 ▼
                                          캘린더 일정  ──이벤트──▶  위젯·메인 창 즉시 갱신
                                              │
                                              ▼
                                       Windows 예약 알림
```

R2를 켠 경우에는 확정 일정만 별도 경로로 복제한다.

```text
[확정 일정] → PC에서 민감정보 재검사 → payload 암호화 → 서명된 동기화 요청
    → [서버: ciphertext + 발송 시각 + 상태 버전] → Web Push → [PWA에서 복호화]
```

### 신뢰 경계

**원문은 `pipeline` 안에서만 존재한다.** 밖으로 나가는 값은 마스킹을 통과한 DTO뿐이다.
저장소에 들어가는 것도 마스킹 이후 값이다. 이 규칙을 어기면 개인정보가 로그·알림·백업에
새어 나간다.

---

## 6. 저장소 설계

### 위치

```
%LOCALAPPDATA%\교사일정도우미\
  schedule.db          SQLite
  content.key          내용 암호화 키 (DPAPI로 감싼 상태)
  fingerprint.key      지문 키 (내용 키와 반드시 다른 값)
```

### 핵심 테이블

| 테이블 | 무엇 |
|---|---|
| `imports` | 가져오기 1회. 파일 지문, 건수, 상태(`staging`/`done`/`failed`) |
| `messages` | 마스킹된 쪽지. 원문 아님 |
| `candidates` | 일정 후보. 신뢰도·모호 표시·보안 표시·자동등록 추천 여부 |
| `calendar_items` | 확정된 일정/할 일/마감 |
| `reminders` | 예약 알림 |
| `preferences` | key-value JSON |
| `undo_log` | 되돌리기 |
| `classification_feedback` | 사용자 교정 학습 |

### 암호화

> R0는 이 열들을 평문으로 저장한다. 아래 암호화는 **R1**부터 적용하며, R0에서 미리 만들어
> 두는 것은 dedupe용 HMAC 지문 키뿐이다 (PRD 15장).

- 제목·장소·메모·출처 요약은 **필드 단위 ChaCha20-Poly1305**
- 두 키는 **DPAPI(CryptProtectData)** 로 감싸 파일에 둔다 → 그 Windows 계정에서만 열린다
- 지문(중복 판정)은 **HMAC** 으로, 내용 키와 **다른 키**를 쓴다. 같은 키를 쓰면 지문에서
  내용을 역추적할 여지가 생긴다
- 백업 파일은 사용자가 정한 복구 암호 + **Argon2id** 로 별도 암호화

### 안전 한도 (넘으면 부분 결과를 확정하지 않고 중단)

| 대상 | 한도 |
|---|---|
| 파일 크기 | 100 MB |
| 시트당 행 | 50,000 |
| 셀 길이 | 100,000 자 |
| 한 행의 칸 | 1,024 |
| 시트 수 | 64 |
| 대기 후보 | 상한을 두고 초과 시 명시적 오류 |
| 파이프라인 시간 | 예산 초과 시 중단 |

---

## 7. 상세 설계

### 7.1 입력 파일 어댑터

**받아들이는 확장자**: `.xls`, `.xlsx` — PRD 6장이 정의하는 입력 범위와 맞춘다. `.xml`은
별도 허용 확장자로 두지 않는다. 쿨메신저가 `.xls`로 내보내는 파일 중 내부가 실제로는
SpreadsheetML 2003 XML인 경우가 있는데(8.1), 그건 확장자가 아니라 **첫 바이트로 판별**해
같은 `.xls` 입력 경로 안에서 처리한다.

**시트**: 이름에 `받은메시지` / `보낸메시지` 가 들어간 시트 (공백 제거 후 비교)

**열 매핑** — 위치가 아니라 **헤더 이름**으로 찾는다. 상단 12행까지 훑어 헤더 행을 찾고,
제목·내용·날짜 중 둘 이상 + 전체 3개 이상을 찾았을 때만 헤더로 인정한다.

| 뜻 | 헤더에 들어 있으면 인정 |
|---|---|
| 구분 | `구분`, `송수신` |
| 상대 | `보낸사람`, `받는사람`, `받은사람`, `발신자`, `수신자` |
| 제목 | `제목` |
| 일시 | `날짜`, `시간`, `일시` |
| 본문 | `내용`, `본문` |
| 첨부 | `첨부` |

모르는 열은 무시하되 **무시했다는 사실을 기록**해 진단 화면에 보여 준다.

**날짜 셀** 형태는 두 가지다. 둘 다 처리한다.

- 엑셀 일련번호(숫자) — 1900/1904 체계를 워크북에서 직접 읽어 계산
- 문자열 `2026/08/24 09:32:21 (월)` — **꼬리 괄호를 먼저 떼고** 해석한다

### 7.1A 사용자 관련성 설정 (역할 태그) — PRD 7.1A

최초 실행에서 개인정보 없이 다음 로컬 역할 태그만 선택한다. 이름·학교명·학번·전화번호는
묻지 않는다. 선택은 건너뛸 수 있다.

- 담임 여부
- 담당 학년
- 보직 여부
- 담당 부서·업무 영역
- 관심 공지(연수·평가·생활지도·진로·정보 등)

이 값은 분류 점수의 **대상적합** 항목(7.2)에만 쓰인다. 값이 비어 있으면 대부분의 공지가
「나와 상관없음」으로 걸러져 자동등록이 0건이 되는데, 이건 정상 동작이다(8.9). 설정 화면에서
언제든 태그를 수정·초기화할 수 있어야 하고, 초기화는 이후 분류에만 영향을 주고 이미 등록된
일정을 건드리지 않는다.

### 7.2 파이프라인 순서

순서를 바꾸면 안 된다. 각 단계는 앞 단계의 결과에 의존한다. **중복 제거를 마스킹보다 먼저
둔다.** 마스킹을 먼저 하면 서로 다른 두 민감 메시지가 똑같은 마스킹 placeholder(예:
`[민감 메시지]`)로 치환된 뒤 그 텍스트로 내용 지문을 만들게 되어, 실제로는 다른 내용인
두 쪽지가 같은 지문으로 오판되어 하나로 합쳐질 수 있다. 순서를 바꾸더라도 원문이 pipeline
밖으로 나가는 시점은 그대로 마스킹 이후이므로(5장 신뢰 경계) 보안 요구사항은 지켜진다.

```
1. 인용문 분리 (인용만 있으면 후보를 만들지 않음)
2. 내용 지문으로 중복 제거 (원문 기준. 지문 계산은 pipeline 안에서만 일어난다)
3. 민감정보 탐지·마스킹
4. 메시지 단위 분류 (통계·격리 판단용)
5. 문장 나누기
6. 날짜·시각·교시 추출  → 날짜 하나마다 후보 씨앗(candidate seed)
7. 문장 단위 분류 + 신뢰도 점수
8. 관계 판정 (변경·취소)
9. 후보 확정 (제목 만들기, 모호 표시 부착, 자동등록 추천 판정)
```

**격리**: 주민번호·계좌·비밀번호·출입번호가 보이면 그 쪽지는 **격리**하고 후보를 만들지
않는다. 미래 날짜가 분명할 때만 「내용 없는 안내 후보」를 만든다.

**두 가지 기간 개념을 헷갈리지 않는다.**

| 이름 | 무엇을 자르는가 | 기본값 |
|---|---|---|
| 가져오기 기간(`window_days`) | **쪽지 발송일**이 이보다 오래되면 아예 읽지 않는다 | 14일 |
| 과거 일정 제외 | 쪽지 안의 **일정 날짜**가 오늘보다 이전이면 후보로 만들지 않는다 | 항상 |

「쪽지는 최근인데 그 안의 날짜가 지났다」가 가장 흔한 경우이고, 이때 후보가 안 생기는 것은
**정상 동작**이다. 후보가 적게 나온다고 파서를 의심하기 전에 이 둘을 먼저 확인한다.

**분류 결과**: `공식일정` / `개인마감` / `개인업무` / `선택행사` / `긴급공지` / `참고공지` /
`단순확인` / `잡담` / `민감` / `불명`

**신뢰도 점수** (0~1). 화면에는 숫자를 보여주지 않고 `높음 / 검토 필요 / 낮음` 세 단계만
보여 준다. 규칙 점수를 확률처럼 보이게 하지 않는다.

```
점수 = 날짜정밀도 + 행동/행사 + 대상적합 + 0.08
       - (지난 날짜면 0.25)
       - (선택 참여 표현이면 0.15)
       - 0.12 × 모호표시 개수

날짜정밀도: 정확한 시각 0.34 / 교시 0.28 / 날짜만 0.26
행동/행사:  내 행동 0.30 / 학교 행사 0.26 / 그 외 0.10
대상적합:   전 교직원·필수 0.28 / 내 역할과 일치 0.20 / 그 외 0.12
```

### 7.3 제목 만들기

쿨메신저 내보내기의 `제목` 열은 **대개 본문 첫 줄이 잘린 것**이다. 그대로 쓰면
`안녕하세요 담임선생님, 교무부 최은지입니다. 학급함에` 같은 인사말이 캘린더에 박힌다.

순서대로 시도한다.

1. 제목 열이 진짜 제목처럼 보이면 그대로 쓴다
2. 아니면 본문에서 「무엇을 + 무엇 한다」를 뽑는다 (`출석부 제출`)
3. 그것도 없으면 분류 이름을 쓴다

`제출함에` 의 `제출` 같은 우연한 일치, 날짜를 지우고 남은 `(목)까지` 같은 부스러기는
제목이 되지 못하게 막는다.

### 7.4 날짜 해석 규칙

기준 시각은 **쪽지 발송일**이다. `오늘/내일/이번 주 화요일`을 그 기준으로 절대화한다.

| 표현 | 해석 | 모호 표시 |
|---|---|---|
| `8월 28일`, `8/28`, `2026-08-28` | 그대로 | 연도 생략 시 학사연도로 추정, 해가 바뀌면 `연도 생략` |
| `오늘/금일/내일/명일/모레/글피` | 발송일 기준 offset | — |
| `이번 주 화요일`, `다음 주 월요일` | 발송일이 속한 주 기준 | — |
| **`금요일까지`, `수요일 창체`** (요일만) | **발송일 이후 가장 가까운 그 요일** | **`요일만 적힘`** |
| `8월 27일 목요일` (날짜+요일 함께, 실제로 일치) | 날짜를 그대로 쓰고 요일은 검증만 한다 | — |
| `8월 27일 금요일` (날짜+요일 함께, 8/27이 실제로는 목요일인 경우) | 자동등록하지 않는다 | `날짜·요일 불일치` |
| `3~4교시` | 교시표가 설정돼 있으면 시각으로 | 없으면 `교시표 미설정` |
| `8/24~8/28` | 시작·종료 | 종료 불명확하면 `종료일 불명확` |
| `오후 3시 30분` | 정확한 시각 | 오전/오후 없이 1~7시면 `오전·오후 불명확` |

**모호 표시(ambiguity flag)의 역할**: 하나라도 붙으면 **자동 등록되지 않는다.** 점수도
깎인다. 후보함에서 사람이 확인한다. 종류는 다음과 같다.

`날짜·요일 불일치` / `연도 생략` / `오전·오후 불명확` / `종료일 불명확` /
`반복 주기 불명확` / `교시표 미설정` / `변경 대상 불명확` / `날짜 범위 벗어남` / `요일만 적힘`

### 7.5 자동 수집 (폴더 감시) [R3]

**설정값**

| 이름 | 기본값 | 설명 |
|---|---|---|
| `enabled` | 켜짐 | 폴더 감시 |
| `folders` | 비었음 → 바탕화면·다운로드 | 감시 폴더 |
| `autoRegister` | 켜짐 | 조건 통과분 캘린더 등록 |
| `level` | `분명한 일정까지` | 등록 기준 (7.6) |
| `pollSeconds` | 5 | 2~600 사이로 제한 |

**동작**

1. 백그라운드 스레드가 `pollSeconds` 마다 폴더를 훑는다
2. 이름이 **`coolmsg_` 로 시작**하고 확장자가 `.xls/.xlsx` 인 것만 본다 (7.1)
3. 마지막 수정이 1.5초 이내면 **아직 쓰는 중**으로 보고 다음 차례로 미룬다
4. 이미 본 파일(경로+크기+수정시각)이면 건너뛴다
5. 오래된 파일부터, 한 번에 최대 5개까지 처리한다
6. 성공·실패 모두 「본 파일」로 기록한다 (같은 오류를 매번 반복하지 않기 위해)
7. 실패는 **조용히 삼키지 않고** 결과에 담아 화면에 보여 준다
8. 기본값은 파일을 지우거나 옮기지 않는 것이다. 첫 설정 화면에서 `성공 후 휴지통으로
   이동 / 파일 유지`를 사용자가 고르게 하고(PRD 6장), 고르면 성공 commit 후에만 이동한다.
   실패한 파일은 정책과 무관하게 항상 그대로 둔다. 영구 삭제는 어떤 경우에도 하지 않는다

**스레드는 꺼져 있어도 계속 돈다.** 매 바퀴 설정을 다시 읽고 꺼져 있으면 아무 일도 하지
않는다. 이렇게 해야 사용자가 설정에서 켠 순간 바로 동작한다 (앱 재시작 불필요).

### 7.6 자동 등록 정책 [R0]

**등록 경로는 손으로 누를 때와 같은 함수를 쓴다.** 자동용 등록 코드를 따로 만들면 교시
해석·알림 예약·되돌리기가 조용히 갈라진다.

**단계와 무관하게 절대 넘지 않는 선** — 하나라도 걸리면 사람이 확인한다.

- 날짜가 없거나 이미 지났다
- 모호 표시가 하나라도 있다
- 민감정보 표시가 있다
- 변경·취소 제안이다 (대상 일정을 사람이 골라야 한다)
- `희망하시는`, `신청자`, `관심 있으신` 같은 선택 참여 표현이 있다
- 분류가 `공식일정`·`개인마감`이 아니다

**움직이는 것은 신뢰도 문턱 하나뿐이다.**

| 단계 | 기준 | 비고 |
|---|---|---|
| `아주 확실한 것만` | 위 조건 + 신뢰도 0.90 이상 | 실제 쪽지에서 0.90을 넘는 일은 드물다. 본문에 `전 교직원 필수` 같은 말이 있어야 넘는다 |
| **`분명한 일정까지`** (기본) | 위 조건만 | |

두 단계가 실제로 다르게 동작하는지 **같은 파일 하나로 반드시 테스트**한다. 같다면 단계를
나눈 의미가 없다.

### 7.7 쿨메신저 UI 자동화 [R3]

쿨메신저에는 밖에서 부를 수 있는 창구가 없다. 그래서 **사람이 누르는 것과 같은 방법**으로
누른다 — Windows UI Automation 으로 단추를 **이름으로** 찾아 Invoke 한다.

**좌표를 쓰지 않는다.** 창 위치·해상도·배율이 바뀌면 좌표는 곧바로 틀린다.

**절차**

```
1. 창 제목으로 쿨메신저를 찾는다. 없으면 실행 파일을 찾아 띄운다
2. 화면이 그려질 때까지 기다린다
     `로그인` 이 보이면      → 로그인 화면
     `메시지 관리함` 이 보이면 → 이미 로그인됨
3. (로그인 화면이면) `로그인` 을 누르고, `메시지 관리함` 이 나타날 때까지 기다린다
4. `메시지 관리함` 을 누른다 → 메시지함은 **별도 창(별도 프로세스)** 으로 열린다
5. 그 창에서 `메시지 다운로드` 를 누른다 → 기간·저장 폴더 대화상자
6. 기본값 그대로 `다운로드` 를 누른다 (기본 저장 위치가 바탕화면 = 감시 폴더)
7. `저장했습니다` 알림창을 **반드시 닫는다**
```

7번을 빠뜨리면 쿨메신저가 멈춘 채로 남는다. 8.2를 반드시 읽는다.

**자동조작이 넘지 않는 선**

- **계정과 비밀번호를 입력하지도, 저장하지도 않는다.** 로그인 화면에서는 저장된 자격 증명을
  채우거나 자동 로그인을 활성화하지 않는다. 공급사가 제공하는 자동 로그인으로 이미 값이 준비된
  경우에만 `로그인`을 한 번 누를 수 있다. 그렇지 않으면 그대로 멈추고 사용자에게 알린다
- 알려진 창 제목·단추 이름이 **예상한 상태로 있을 때만** 누른다. 하나라도 어긋나면 멈춘다
- **무한 재시도를 하지 않는다.** 단계마다 시간 제한을 두고, 실패하면 그 몫은 끝낸다
- 잠금 화면, 전체 화면 발표 중에는 시작하지 않고 다음 시각으로 미룬다
- 실패하면 **수동 가져오기 안내로 내려앉는다.** 폴더 감시는 그대로 동작하므로
  「내려받기만 손으로」 상태가 되고, 기능 전체가 죽지 않는다
- 파이프라인·보안 규칙을 **우회하지 않는다.** 파일을 얻는 방법만 다를 뿐, 그 뒤는 손으로
  가져왔을 때와 완전히 같은 경로다

**실행 파일 찾는 순서**: 설정에 적힌 경로 → 지정된 위치 → `Program Files`·
`Program Files (x86)` 안에서 이름이 `CoolMessenger` 로 시작하는 폴더. 설치 폴더 이름이
제품마다 다르므로 이름을 못박지 않는다.

**예약 실행**

| 규칙 | 이유 |
|---|---|
| 시각을 여러 개 정할 수 있다 (`08:00, 12:30`) | |
| 주말 제외 옵션 (기본 켜짐) | 학교다 |
| **놓친 시각은 켠 뒤 한 번 따라잡는다** | 컴퓨터가 꺼져 있었다고 그날 치를 건너뛰면 정해 둔 의미가 없다 |
| **여러 개를 놓쳐도 한 번만 한다** (가장 늦은 몫) | 창이 세 번 뜨면 안 된다 |
| **시각을 먼저 기록하고 내려받는다** | 실패해도 그 몫은 끝. 안 그러면 쿨메신저 없는 PC에서 5초마다 창을 띄우려 든다 |
| 기본값 꺼짐 | 창이 뜨고 포커스를 가져가는 동작이라 사용자가 직접 켜야 한다 |

### 7.8 바탕화면 위젯 [R1]

- 창 하나. `달력` / `목록` 을 같은 창에서 전환한다. **창을 둘로 만들지 않는다** —
  작은 창이 둘이면 서로 겹치고 위치·크기를 각각 기억해야 해서 문제가 배로 는다
- `transparent` + `decorations:false` + **`shadow(false)`**
- 실제 투명도는 CSS가 정한다. 창 전체에 알파를 먹이면 글자까지 흐려져 대비를 못 맞춘다
- 투명도 60~90% 범위 강제 (그 밖은 배경 위에서 글자를 읽을 수 없다)
- 크기 260~420 × 56~560 범위 강제
- `skip_taskbar: true`
- 항상 위 = 기본 꺼짐

### 7.9 부팅 시 자동 실행 [R1]

`HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 에 **사용자별 항목만** 만든다
(관리자 권한 불필요, 다른 계정에 영향 없음).

등록 명령: `"<실행파일 전체 경로>" --widget-only`

`--widget-only` 로 켜지면 메인 창을 띄우지 않고 위젯만 띄운다.

**위젯을 켜면 이 등록도 함께 켠다.** 「바탕화면 위젯 사용」을 켠 사람의 기대는
*컴퓨터를 켜면 위젯이 있다* 이다. 등록이 설정 화면의 다른 스위치라면 그 존재를 알아야만
기능이 성립한다는 뜻이고, 그것 자체가 설계 실패다. 단 **사용자가 자동 실행 스위치를 직접
만진 적이 있으면 그 선택을 이기지 않는다.**

### 7.10 화면 즉시 반영 [R1]

**주기적으로 다시 읽는 것만으로는 안 된다.** 저장했는데 위젯이 그대로면 사용자는
「저장이 안 됐다」고 본다. 주기를 줄이는 것은 답이 아니다 — 그만큼 계속 저장소를 읽게 되고,
그래도 「바로」는 되지 않는다.

**바뀌는 쪽에서 알린다.**

| 신호 | 보내는 곳 | 듣는 곳 |
|---|---|---|
| `schedule-changed` | 일정을 바꾸는 모든 호출(등록·수정·삭제·되돌리기·가져오기)이 **성공했을 때** | 위젯 |
| `autocollect` | 자동 수집이 파일을 처리했을 때 (Rust 백그라운드 스레드) | 위젯 + 메인 창 + 설정 화면 |

주기적 갱신(60초)은 **날짜가 바뀌는 경우**를 위한 뒷받침으로만 남긴다.

### 7.11 알림 [R0]

Windows 예약 알림(Toast)을 쓴다. **앱이 꺼져 있어도 도착한다.**

후보 구현은 다음 두 등록을 사용하지만, 이것만으로 모든 설치 환경에서 충분하다고 가정하지 않는다.

1. `HKCU\Software\Classes\AppUserModelId\{AUMID}` — 알림 센터 표시 이름
2. 시작 메뉴 바로가기(.lnk)에 AUMID 속성

프로세스 AUMID 지정은 **창을 만들기 전에** 해야 한다.

R0 초기에 실제 NSIS 설치본으로 `앱 종료 / 재부팅 / 절전 복귀 / 알림 클릭 / 수정·삭제 취소`를
Windows 10·11에서 검증한다. 클릭으로 앱을 활성화해야 하는 경우 패키징 방식에 맞는 활성화 등록을
추가한다. 이 스파이크를 통과하기 전에는 알림 구현 경로를 완료로 판정하지 않는다.

OS 예약을 쓸 수 없는 환경이면 앱 내 폴백으로 내려가되, **그 사실을 사용자에게 분명히
알린다** (앱이 켜져 있을 때만 온다는 뜻이므로).

### 7.12 휴대폰 연결·동기화 [R2] (이번 구현 범위 제외)

> **2026-08-27 결정:** 로컬 전용 확정으로 이번 프로젝트에서는 구현하지 않는다. 삭제하지
> 않고 향후 재개 시를 위한 설계로만 남긴다.

R2의 세부 보안·보존 요구사항과 서버 데이터 모델은 PRD 8장, 12.1, 12A, 15장을 기준으로 한다.
구현자는 다음 인터페이스와 상태 전이를 먼저 ADR로 고정하고 임의의 장기 토큰이나 평문 필드를
추가하지 않는다.

```text
pairing: issued(5분) → claimed → confirmed → revoked/expired
schedule: create/update/cancel → state_version 증가 → 발송 직전 최신 버전 확인
device: active → rotating → revoked → server data deleted
```

- PC는 기기별 서명 key pair를 만들고 private key를 DPAPI 또는 Credential Manager로 보호한다.
- QR의 pairing token은 5분 뒤 폐기하며 암호화 키는 URL fragment 등 서버 비가시 채널로 전달한다.
- 동기화 API는 nonce·timestamp·body hash·서명을 검증하고 재사용 nonce를 거부한다.
- 서버는 `ciphertext`, `remind_at`, 기기·일정 식별자, 상태 버전과 최소 발송 상태만 저장한다.
- 제목·장소·메모·원문·발신자명은 서버 DB·로그·작업 큐에 평문으로 넣지 않는다.
- Cloud Tasks는 30일 이내 알림만 넣고 장기 일정은 일일 materializer가 발송 가능 구간에 들어올
  때 생성한다. 작업 키는 `(device_id, reminder_id, remind_at)`으로 멱등 처리한다.
- 수정·완료·삭제·기기 해제는 상태 버전을 먼저 올린다. 큐 취소가 실패해도 오래된 작업은 발송
  직전 버전 검증에서 종료한다.
- 서버 삭제가 확인된 뒤 로컬 키를 지운다. 네트워크 실패 시 삭제 재시도용 최소 tombstone만 남긴다.
- PWA는 iPhone/iPad에서 홈 화면 추가와 사용자 동작에 의한 알림 권한 요청을 안내하고 기능 탐지로
  지원 여부를 판정한다.

R2 구현 완료 전 `API_SCHEMA.md`, `THREAT_MODEL_R2.md`, Firestore rules 테스트, 키 회전·기기 해제·
오래된 작업 차단 통합 테스트를 제출한다.

---

## 8. 반드시 지켜야 할 제약

**이 장이 이 문서의 핵심이다.** 아래 항목은 전부 「모르면 반드시 걸리고, 알면 5분이면
피하는」 함정이다. 각 항목은 `규칙 / 이유 / 확인법` 으로 쓴다.

### 8.1 `.xls` 확장자가 같아도 내부 형식이 다르다 ★★★

**규칙**: 확장자를 믿지 말고 **첫 바이트를 읽어 판별**한다. 최소 두 형식을 모두 읽어야 한다.

| 첫 바이트 | 실제 형식 | 읽는 방법 |
|---|---|---|
| `D0 CF 11 E0 A1 B1 1A E1` | OLE2 복합 문서 (구형 진짜 xls) | calamine |
| `50 4B 03 04` | ZIP (xlsx) | calamine |
| `EF BB BF 3C 3F 78 6D 6C` (BOM + `<?xml`) | **SpreadsheetML 2003 XML** | **직접 파싱** |

**이유**: calamine은 OLE2와 ZIP만 읽는다. SpreadsheetML은 겉이 `.xls`인 XML 텍스트다.
이걸 처리하지 않으면 「쿨메신저에서 받은 파일인데 앱이 못 읽는다」가 된다. 이건 데모
첫 화면에서 막히는 사고다.

**SpreadsheetML 구조**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<?mso-application progid="Excel.Sheet"?>
<Workbook xmlns="urn:schemas-microsoft-com:office:spreadsheet"
          xmlns:ss="urn:schemas-microsoft-com:office:spreadsheet">
  <Worksheet ss:Name="받은메시지"><Table>
    <Row><Cell><Data ss:Type="String">구분</Data></Cell> ... </Row>
  </Table></Worksheet>
  <Worksheet ss:Name="보낸메시지"> ... </Worksheet>
</Workbook>
```

판별은 파일 앞 4KB만 읽어 `<?xml` 또는 `<Workbook` 으로 시작 + 네임스페이스 문자열 포함
여부로 한다. 전체를 메모리에 올리지 않는다.

**파서가 지켜야 할 것**

- 네임스페이스 접두어를 떼고 태그·속성 이름을 비교한다 (`ss:Name` 과 `Name` 을 같게)
- `<Cell/>` 처럼 **스스로 닫는 태그는 End 이벤트가 오지 않는다.** 별도로 처리한다
- **`ss:Index` 를 존중한다.** 빈 칸을 건너뛰는 표준 방식이다. 무시하면 열이 밀려
  제목 자리에 날짜가 들어간다
- `ss:Index="900000"` 같은 값으로 행 한도를 뛰어넘지 못하게 막는다
- 셀 길이·행 수·시트 수 한도를 넘으면 **부분 결과를 쓰지 않고** 오류
- 깨진 XML은 조용히 반쪽 결과를 만들지 말고 오류

**결과를 calamine의 `Range<Data>` 로 돌려준다.** 그러면 헤더 찾기·셀 해석·날짜 파싱을
**두 벌로 만들지 않아도 된다.** 읽는 방법만 갈라지고 그 뒤 경로는 하나다.

**확인법**: 두 형식 파일을 각각 넣어 같은 건수·같은 내용이 나오는지 본다.

### 8.2 WebView2 앱은 신호를 받아야 화면 내용을 내놓는다 ★★★

**규칙**: UI Automation으로 창을 읽기 **전에** 그 창에
`WM_GETOBJECT(wParam=0, lParam=OBJID_CLIENT= -4)` 를 보낸다.

**이유**: Chromium(WebView2)은 보조 기술이 붙었다는 신호를 받기 전에는 접근성 트리를
만들지 않는다. 그 전에는 **창 테두리 단추(최소화·최대화·닫기)만 보이고** 화면 안의
`로그인` 은 존재하지 않는 것처럼 보인다. 요소 수가 22개와 52개로 갈린다.

이걸 모르면 「단추를 찾지 못했습니다」로 timeout까지 기다리다 실패하는데, 정작 사람 눈에는
그 단추가 화면에 뻔히 보인다. 원인을 찾기 매우 어렵다.

**확인법**: 신호 전후로 `FindAll(TreeScope_Descendants, TrueCondition)` 개수를 찍어 본다.
신호 전에는 창 테두리 단추 몇 개뿐이고, 신호 뒤에는 화면 안의 모든 요소가 나온다.
개수가 몇 배로 뛰지 않으면 신호가 안 먹은 것이다.

### 8.3 UI 자동화에서 멈추는 두 가지 ★★★

**(가) COM 아파트먼트**

**규칙**: `CoInitializeEx(None, COINIT_MULTITHREADED)` — **MTA로 연다.**

**이유**: STA(`COINIT_APARTMENTTHREADED`)로 열면 UI Automation의 프로세스 간 호출이
메시지 펌프를 기다린다. 백그라운드 스레드에는 펌프가 없으므로 **첫 호출에서 영원히
멈춘다.** 진행 로그가 없으면 무한 대기로 보이고 원인을 못 찾는다.

이미 다른 모드로 초기화된 스레드면 `RPC_E_CHANGED_MODE (0x80010106)` 가 오는데,
그건 오류로 보지 말고 그대로 진행한다.

**(나) 메시지 전송**

**규칙**: `SendMessageW` 대신 **`SendMessageTimeoutW` + `SMTO_ABORTIFHUNG` + 1초 제한.**

**이유**: `SendMessageW` 는 상대 창의 스레드가 그 메시지를 처리할 때까지 **무한정
기다린다.** 쿨메신저가 알림창을 띄우고 멈춰 있으면 이쪽도 함께 멈춘다. 8.2의 신호를
보내는 그 호출이 바로 여기에 해당한다.

### 8.4 UI 자동화에서 어긋나는 세 가지 ★★

**(가) 단추 이름은 정확히 일치하지 않는다**

**규칙**: 이름은 **접두어로** 찾는다.

**이유**: 메시지함 단추 이름이 `메시지 관리함, 안 읽은 메시지 5건` 이다. 숫자가 계속
바뀐다. 정확히 일치만 보면 쪽지 하나 오는 순간 자동화가 깨진다.

**(나) 창 제목은 내용보다 먼저 생긴다**

**규칙**: 「단추가 있나」를 **한 번만 보고 판단하지 않는다.** 여러 후보 단추 중 먼저
나타나는 것을 기다려 지금 상태를 판정한다.

```
로그인 화면인가 이미 로그인됐는가를 `로그인` / `메시지 관리함` 중
무엇이 먼저 나타나는지로 정한다
```

**이유**: 창이 뜬 직후 한 번만 보면 `로그인` 이 아직 없다. 그러면 「이미 로그인돼 있다」고
잘못 판단하고 다음 단계에서 timeout까지 기다리다 실패한다.

**(다) 알림창은 다른 창이다**

**규칙**: 저장 완료 알림(`#32770` 클래스, 제목은 대화상자와 같음)을 **창 제목으로 찾아
`WM_CLOSE` 로 닫는다.**

**이유**: 이 알림창은 자식 컨트롤이 UI Automation 트리에 없어 `확인` 단추를 이름으로
누를 수 없다. 그리고 닫지 않으면 **쿨메신저가 멈춘 채로 남는다.** 다음 실행도 실패한다.
닫힌 것을 `IsWindow` 로 확인하고, 안 닫혔으면 다시 시도한다.

### 8.5 자동 수집에서 반드시 지킬 것 ★★★

**(가) 「이미 본 파일」 기록과 저장소는 함께 움직여야 한다**

**규칙**: 두 가지를 모두 한다.

1. 자료를 모두 지울 때 「이미 본 파일」 기록도 **같은 트랜잭션에서** 지운다
2. 가져오기 기록이 **0건인데** 파일 기록이 남아 있으면 그 기록을 **믿지 않고 지운다**

**이유**: 이 기록이 살아남으면 앱은 폴더의 파일을 「이미 본 것」이라며 건너뛴다.
**캘린더는 비어 있는데 파일은 영영 안 들어오고, 사용자가 할 수 있는 일이 없다.**
재설치·데이터 삭제 뒤에 반드시 발생한다. 2번이 없으면 이미 그 상태가 된 설치본은
복구되지 않는다.

**(나) 남의 파일을 열지 않는다**

**규칙**: 이름이 `coolmsg_` 로 시작하는 파일만 본다. 확장자만 보지 않는다.

**이유**: 선생님 바탕화면에는 성적 파일도 있고 명렬표도 있다.

**(다) 쓰는 중인 파일을 읽지 않는다**

**규칙**: 마지막 수정이 1.5초 이내면 건너뛰고 다음 차례에 다시 본다.

**이유**: 다 쓰이기 전에 읽으면 잘린 파일을 「깨진 파일」로 판정해 버리고, 그 파일은
「본 파일」로 기록돼 다시 시도되지 않는다.

### 8.6 부팅 자동 실행에서 어긋나는 네 가지 ★★

**(가) 등록 경로를 매번 덮어쓰지 않는다**

**규칙**: 등록된 명령줄을 **읽어서 지금 실행 파일과 다를 때만** 다시 쓴다.
그리고 **개발 빌드에서는 갱신하지 않는다.**

**이유**: 무조건 덮어쓰면, 설치본으로 등록해 둔 PC에서 개발 빌드를 한 번 켜는 순간 등록이
`target\debug\...` 를 가리킨다. `cargo clean` 한 번에 그 경로가 사라져 **그때부터 부팅해도
아무 것도 뜨지 않는다.** 원인을 찾기 어렵다.

**(나) 큰 창이 번쩍이지 않게 한다**

**규칙**: 메인 창을 설정에서 **`visible: false`** 로 만들고, 자동 실행이 아닐 때만 코드로
보여 준다. 창을 보여 주는 경로를 **함수 하나로 모은다.**

**이유**: 보이게 만든 뒤 시작 훅에서 숨기면 그 사이에 1180×780 창이 한 번 떴다 사라진다.

**(다) 보이지 않는 프로세스를 남기지 않는다**

**규칙**: `--widget-only` 로 켜졌는데 위젯 창을 만들지 못하면 **메인 창을 띄운다.**

**이유**: 그러지 않으면 메인 창도 숨어 있고 작업표시줄에도 없어 **화면에 아무 것도 없는
채로 프로세스만 돈다.** 사용자가 손댈 방법이 없다.

**(라) 중복 실행을 막고, 두 번째 실행은 첫 창을 꺼내 준다**

**규칙**: 이름 있는 뮤텍스(`Local\...`)로 한 인스턴스만 띄운다. 두 번째 실행은
`FindWindowW` 로 먼저 뜬 창을 찾아 `ShowWindow(SW_RESTORE)` + `SetForegroundWindow`
한 뒤 조용히 물러난다.

**이유**: 위젯은 작업표시줄에 없다. 메인 창을 닫은 뒤 시작 메뉴에서 다시 실행하면 두 번째
인스턴스가 떠서 **위젯이 같은 자리에 겹친다.** 사용자 눈에는 하나인데 닫아도 아래 것이
남아 「닫히지 않는 창」이 되고, 두 프로세스가 같은 DB를 두고 잠금을 다툰다.

### 8.7 위젯 창에서 어긋나는 네 가지 ★★

**(가) 좌표는 논리 픽셀로 저장한다**

**규칙**: `outer_position`·`inner_size` 는 **물리 픽셀**을 돌려준다. 창을 만들 때 쓰는
`position`·`inner_size` 는 **논리 픽셀**을 받는다. 저장 전에 `scale_factor` 로 나눈다.

**이유**: 그대로 저장하면 껐다 켤 때마다 배율(예: 125%)만큼 커지고 위치도 밀려난다.
몇 번 만에 창이 화면 밖으로 나간다.

**(나) 창을 없애지 말고 숨긴다**

**규칙**: 위젯을 끌 때 창을 파괴하지 않고 `hide()` 한다. 켤 때 이미 있으면 `show()`.

**이유**: 없앤 직후에는 창이 목록에 잠시 남아 있어 「이미 있다」고 판단하고 죽은 창에
`show()` 를 건다. 껐다 켜면 아무 것도 안 뜬다. 같은 이름으로 다시 만들면 라벨 충돌 오류가
난다.

**(다) 저장된 좌표가 지금 화면 밖일 수 있다**

**규칙**: 저장된 위치가 상식적인 범위(예: x가 -200~8000, y가 -40~8000)를 벗어나면 무시하고
안전한 기본 자리에 띄운다. 크기도 범위를 벗어나면 **경계값으로 깎지 말고 기본 크기로
되돌린다.**

**이유**: 모니터를 뽑았거나 해상도가 바뀌면 예전 좌표가 화면 밖을 가리킨다. 그러면
「켜기를 눌러도 아무 것도 안 뜬다」가 된다. 크기를 경계값으로 깎지 않는 이유는, 범위를
벗어날 만큼 커진 것은 사용자가 원해서가 아니라 모서리를 잘못 끈 결과이고 그때 원하는 것은
「원래 크기로 돌아가기」이기 때문이다.

**(라) 동기 명령 안에서 이벤트 루프에 묻지 않는다**

**규칙**: 동기 command 안에서 `available_monitors()` 같이 이벤트 루프에 물어보는 API를
부르지 않는다. 저장된 좌표가 쓸 만한지는 **숫자 범위로만** 판단한다.

**이유**: 동기 command는 메인 스레드에서 돈다. 그 안에서 이벤트 루프에 물으면 자기 자신을
기다리다 멈춘다. 증상은 「위젯 켜기를 눌러도 안 뜬다」, 「파일 선택 창이 안 열린다」,
「화면이 비어 있다」 세 가지로 흩어져 나타나 원인을 찾기 어렵다.

### 8.8 오래 걸리는 명령은 비동기로 ★★

**규칙**: 쿨메신저 자동화처럼 수십 초 걸릴 수 있는 명령은 `async` command + 작업 스레드
(`spawn_blocking`) 로 보낸다.

**이유**: 동기 command는 메인 스레드에서 돈다. 쿨메신저가 뜨기를 기다리는 동안 **앱 화면이
통째로 얼어붙는다.**

### 8.9 역할 설정이 비어 있으면 대부분 걸러진다 ★★

**규칙**: 담당 학년·담임 여부가 비어 있으면 **설정 화면에서 경고를 띄운다.**

**이유**: 이 값이 비어 있으면 파이프라인이 대부분의 공지를 「나와 상관없는 것」으로 보고
걸러 낸다. 같은 파일에서 후보가 눈에 띄게 줄고 자동 등록은 0건이 된다.
「가져왔는데 캘린더가 비어 있다」의 가장 흔한 원인이며, 코드 오류가 아니라서 더 찾기 어렵다.

### 8.10 요일만 적힌 마감을 놓치지 않는다 ★★

**규칙**: `금요일까지`, `수요일 창체` 처럼 **날짜 없이 요일만** 적힌 표현을 날짜로
해석한다 (7.4).

**이유**: 학교 쪽지에서 가장 흔한 마감 표현이다. 이걸 빠뜨리면 「제출·실시」가 걸린
진짜 업무가 통째로 사라진다. 날짜 인식 실패의 상당수가 이 형태다.

**동시에**: 같은 문장에 이미 날짜가 있으면 그 요일은 **그 날짜의 요일 표기일 뿐**이므로
"요일만 적힘" 추출 로직을 중복 적용해 별도 일정을 만들지 않는다 (`8월 27일 목요일 회의`
→ 일정 하나). **단, 이게 요일 불일치 검증을 생략해도 된다는 뜻은 아니다.** 8/27이 실제로
목요일이 아니면 PRD 7.1과 7.4 표대로 `날짜·요일 불일치`로 걸러 자동등록하지 않는다. "중복
추출 금지"와 "불일치 검증"은 별개의 규칙이다.

### 8.11 달력 칸 너비가 내용에 흔들린다 ★

**규칙**: 달력 격자는 `repeat(7, minmax(0, 1fr))` 로 만든다. 칸 안의 일정 막대는
`display: flex` 로 두지 않는다.

**이유**: `1fr` 은 사실 `minmax(auto, 1fr)` 이라 **긴 제목이 열을 밀어낸다.** 요일 열 너비가
그날 일정 제목 길이에 따라 달라진다. 그리고 flex 컨테이너에는 `text-overflow: ellipsis` 가
먹지 않아 넘친 제목이 잘리지 않는다. 한 줄 블록으로 두어야 `...` 로 줄어든다.

**확인법**: 눈으로 보지 말고 일곱 열의 X 좌표 간격을 재서 같은지 본다.

### 8.12 IPC 이름은 컴파일러가 잡아 주지 않는다 ★

**규칙**: 화면이 부르는 명령 문자열과 Rust에 등록된 명령 목록을 **대조하는 검사**를 만들어
두고 CI/작업 끝마다 돌린다.

**이유**: `invoke("...")` 의 오타는 런타임에야 터진다. 명령이 50개를 넘어가면 사람 눈으로
못 잡는다.

### 8.13 Windows에서의 잔가시 ★

- PowerShell 5.1은 `.ps1` 을 ANSI로 읽는다. 한글이 든 스크립트는 **UTF-8 BOM**으로 저장한다
- 배포용 문서(PDF)는 손으로 인쇄하지 말고 **명령 하나로 다시 만드는 스크립트**를 만든다.
  손으로 만들면 원본을 고쳐도 산출물이 따라오지 않아 내용이 어긋난 채 배포된다
- 설치 파일 해시를 안내 문서에 적는다면 **해시를 먼저 고치고 그다음 문서를 만든다.**
  순서를 바꾸면 옛 해시가 인쇄된다

---

## 9. 테스트 계획

**단위 테스트로는 못 잡는 것이 이 앱의 절반이다.** 아래 세 층을 모두 만든다.

### 9.1 단위 테스트 (빠름, 많이)

- 날짜 파싱: 형식별 정상/경계/모호 표시 부착
- 분류·점수: 각 분기와 점수 상한/하한
- 제목 만들기: 인사말 제거, 부스러기 제거
- 시그니처 판별: 세 형식 + 남의 XML을 거절하는가
- SpreadsheetML 파서: `ss:Index`, 자기 닫는 태그, 한도 초과, 깨진 XML
- 예약 시각 판정: 지남/따라잡기/중복 방지/주말/잘못된 값
- 자동 등록 게이트: 각 안전 조건이 실제로 막는가
- 위젯 좌표·크기 범위 강제

### 9.2 통합 테스트 (실제 파일로)

**입력은 반드시 쿨메신저가 실제로 내보낸 파일 바이트를 fixture로 넣는다.**
손으로 만든 가짜 XML로는 형식이 바뀌었을 때 깨지지 않는다.

반드시 있어야 하는 테스트:

| 테스트 | 확인하는 것 |
|---|---|
| 폴더에 파일을 두면 후보가 된다 | 자동 수집 기본 동작 |
| 자동 등록을 켜면 조건 통과분만 캘린더에 들어간다 | 전부 넣지 않는다 |
| **조건을 갖춘 쪽지 하나가 실제로 자동 등록된다** | 게이트가 잠긴 것과 대상이 없는 것을 구분 |
| 같은 파일을 두 번 가져오지 않는다 | 중복 방지 |
| 이름이 다른 엑셀은 건드리지 않는다 | 남의 파일 보호 |
| 깨진 파일 하나가 나머지를 막지 않는다 | 실패 격리 |
| 쓰는 중인 파일은 다음 차례로 미룬다 | 잘린 파일 방지 |
| **자료를 지우면 파일이 다시 들어온다** | 8.5(가) 회귀 |
| **기록만 남고 저장소가 비면 스스로 고친다** | 8.5(가) 회귀 |
| 두 등록 단계가 실제로 다르게 동작한다 | 단계 구분이 의미 있는가 |

「조건을 갖춘 쪽지 하나」 테스트가 특히 중요하다. 이게 없으면 **자동 등록이 영영 일어나지
않는 죽은 기능인지, 그냥 데이터에 대상이 없는 것인지 구분할 수 없다.**

### 9.3 실기 확인 (사람이 직접)

자동화할 수 없으므로 체크리스트로 남긴다.

- [ ] 쿨메신저를 끈 상태에서 「지금 받아오기」 → 파일 생성 → 캘린더 반영
- [ ] 쿨메신저가 이미 떠 있고 로그인된 상태에서 같은 동작
- [ ] 받아온 뒤 쿨메신저가 **멈춰 있지 않은지** (알림창이 닫혔는지)
- [ ] 예약 시각을 1분 뒤로 맞추고 기다려 본다
- [ ] **실제로 재부팅** — 위젯만 뜨고 메인 창은 한 번도 보이지 않는가
- [ ] 그 상태에서 시작 메뉴로 앱을 한 번 더 실행 → 첫 창이 앞으로 나오는가
- [ ] 일정 저장·삭제 시 위젯이 **1초 안에** 바뀌는가
- [ ] 데이터 모두 삭제 후 폴더의 파일이 다시 들어오는가

창 상태 확인은 눈으로만 하지 말고 `EnumWindows` 로 창 목록과 표시 여부를 찍어 남긴다.

---

## 10. 구현 순서

의존 관계 순서대로. 각 단계가 끝나면 **그 단계까지만으로 동작을 확인**한다.
Gate 열은 0장 대응표를 그대로 따른다.

| 단계 | Gate | 내용 | 끝났다는 뜻 |
|---|---|---|---|
| 1 | R0 | 저장소·스키마·키 보호 | 앱이 뜨고 DB가 만들어진다 |
| 2 | R0 | 어댑터 (**두 형식 모두**) | 파일을 넣으면 쪽지 건수가 나온다 |
| 3 | R0 | 파이프라인 (인용분리→중복제거→마스킹→분류→날짜→후보, 7.2) | 후보 목록이 나온다 |
| 4 | R0 | 화면: 캘린더·후보함·설정(역할 태그 포함) | 손으로 가져오기·등록이 된다 |
| 5 | R0 | 자동 등록 | 조건 통과분이 캘린더에 들어간다 |
| 6 | R0 | 알림 | 예약 알림이 온다 |
| 7 | R0 | golden set·단위·통합 테스트, README | PRD 20장 R0 인수 기준을 표로 제출 |

**여기서 멈추고 R0 결과에 대한 사용자 승인을 기다린다 (PRD 0장 8항).**
승인 없이 아래 단계로 넘어가지 않는다.

| 단계 | Gate | 내용 | 끝났다는 뜻 |
|---|---|---|---|
| 8 | R1 | 위젯 | 바탕화면에 뜬다 |
| 9 | R1 | 부팅 자동 실행 | 재부팅해도 위젯이 뜬다 (시나리오 C) |
| 10 | R1 | 즉시 반영 이벤트 | 저장하면 위젯이 바로 (시나리오 D) |
| 11 | R1 | 백업·복원·진단 | |

**여기서 다시 멈추고 R1 결과에 대한 승인을 기다린다. 이번 구현에서는 R2를 건너뛰고 R3로 간다.**

| 단계 | Gate | 내용 | 끝났다는 뜻 |
|---|---|---|---|
| 12 | R3 | 자동 수집 (폴더 감시) | 폴더에 파일을 두면 저절로 후보가 된다 |
| 13 | R3 | 쿨메신저 UI 자동화 | 단추 하나로 받아온다 (시나리오 A) |
| 14 | R3 | 예약 실행 | 정한 시각에 저절로 (시나리오 B) |

**R0 안에서는 1~7이 핵심이다.** 해커톤처럼 R0만 시연한다면 5(자동 등록)와 6(알림)을
가장 나중에 버린다 — 그래도 "쪽지를 넣으면 후보가 나오고 손으로 등록된다"는 남는다.
R3까지 갈 시간이 있다면 12~13이 이 제품의 최종 차별점이다.

### 향후 확장 순서 — R2 (이번 구현 및 테스트 대상 아님)

R2를 다시 추진하기로 별도 결정한 경우에만 `7.12`와 PRD의 R2 사양을 기준으로 다음 단계를
새 Release Gate에서 수행한다.

1. 모바일 PWA·일회용 QR 연결
2. 암호화 동기화·Web Push·장기 일정 materializer
3. 기기 해제·키 회전·서버 삭제·위협 모델 테스트
4. R2 인수 결과 제출과 별도 승인

---

## 11. 수용 기준

각 항목은 사람이 눈으로 확인할 수 있는 문장이다. **아래 "핵심" 중 위젯·자동 실행·자동조작
관련 항목은 R1·R3 기준이며 R0 승인 시점에는 아직 대상이 아니다.** R0 승인 시점의 공식
기준은 PRD 20장 "R0 완료" 표다. "정확성"·"견고함"·"안전" 항목은 대부분 R0부터 적용된다.

**핵심**

- [ ] 이미 로그인됐거나 공급사 자동 로그인이 설정된 쿨메신저를 끈 상태에서 단추 하나로 내려받아 캘린더까지 반영된다
- [ ] 자격 증명이 준비되지 않은 로그인 화면에서는 입력·클릭을 중단하고 사용자에게 안내한다
- [ ] 정해 둔 시각에 위 동작이 저절로 일어난다
- [ ] 재부팅 후 앱을 실행하지 않아도 위젯이 떠 있고, 메인 창은 뜨지 않는다
- [ ] 일정을 바꾸면 위젯이 1초 안에 따라온다

**R2 향후 확장 수용 기준 — 이번 구현 및 테스트 대상 아님**

- [ ] QR로 연결한 지원 휴대폰에서 PC가 꺼져 있어도 예약된 모바일 알림을 받는다
- [ ] 기기 연결 해제 후 서버의 예약 알림과 Push Subscription이 삭제된다

**정확성**

- [ ] OLE2 형식과 SpreadsheetML 형식 파일이 **같은 결과**를 낸다
- [ ] 요일만 적힌 마감이 후보로 잡히고, 자동 등록되지는 않는다
- [ ] 모호·민감·변경취소·선택행사·지난 날짜는 자동 등록되지 않는다
- [ ] 이름이 `coolmsg_` 가 아닌 엑셀 파일은 열지 않는다

**견고함**

- [ ] 자료를 모두 지운 뒤 폴더의 파일이 다시 들어온다
- [ ] 깨진 파일 하나가 나머지 파일 처리를 막지 않고, 실패가 화면에 보인다
- [ ] 쿨메신저가 없는 PC에서 예약 시각이 와도 창을 반복해 띄우지 않는다
- [ ] 자동조작이 계정·비밀번호를 입력하거나 저장하지 않는다
- [ ] 자동조작이 실패해도 폴더 감시는 계속 동작한다
- [ ] 자동화 중에도 앱 화면이 얼지 않는다
- [ ] 같은 시각 몫을 두 번 실행하지 않고, 놓친 몫은 한 번만 따라잡는다

**안전**

- [ ] R2를 끈 상태에서 교사 일정도우미 프로세스가 직접 생성하는 외부 네트워크 요청이 0건이다. 쿨메신저·OS·WebView2의 자체 통신은 프로세스별로 분리해 측정한다
- [ ] 저장소에 원문이 남지 않는다 (마스킹 통과값만)
- [ ] 주민번호·계좌·비밀번호가 든 쪽지는 격리되고 후보가 되지 않는다

---

## 12. 배포

- 설치 파일: NSIS, **사용자 계정 설치**(관리자 권한 불필요)
- 일반 인터넷 환경용 설치본은 WebView2 `downloadBootstrapper` 또는 `embedBootstrapper`를 사용한다
- 폐쇄망용 설치본은 WebView2 `offlineInstaller`를 포함한다. 약 127MB가 추가될 수 있으므로 파일명·다운로드 화면·안내 문서에 `오프라인 설치본`과 실제 크기를 명시한다
- `embedBootstrapper`도 WebView2 Runtime 설치에는 인터넷이 필요하므로 폐쇄망 설치본으로 표시하지 않는다
- 코드 서명이 없으면 SmartScreen 파란 경고가 뜬다. **왜 뜨는지, 무엇이 아닌지**를 안내
  문서에 먼저 쓴다 (「위험하다」가 아니라 「만든 사람을 확인할 수 없다」는 뜻)
- 서명 대신 **SHA-256 해시**를 안내 문서에 적어 전달 도중 변조를 확인할 수 있게 한다
- 백신이 막으면 **설정을 바꾸지 말고 알려 달라**고 안내한다. 학교 PC의 보안 설정을 낮추게
  하지 않는다

**앱이 스스로 하는 일은 사용 안내와 동의 문서에 그대로 적는다.**
어떤 조건일 때 등록하고 무엇은 등록하지 않는지, 어떻게 끄는지까지. 「앱이 마음대로 일정을
만들지 않습니다」라고 적어 두고 만드는 일은 하지 않는다.

---

## 13. 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| 쿨메신저 화면 구성이 바뀐다 | UI 자동화가 깨진다 | 단추를 이름 접두어로 찾고, 실패 시 어느 단계에서 막혔는지 남긴다. 폴더 감시는 그대로 동작하므로 「내려받기만 손으로」로 자연히 내려앉는다 |
| 실제 내보내기 형식이 예상과 다르다 | 파일을 못 읽는다 | 시그니처로 판별하고 모르는 형식은 명확한 오류. 헤더는 위치가 아니라 이름으로 찾는다 |
| 쪽지 데이터의 날짜가 이미 지났다 | 후보가 거의 안 생긴다 | 정상 동작이다. 시연용 데이터는 **시연 당일 기준으로 미래 날짜**가 들어가도록 준비한다 |
| 자동 등록이 오답을 넣는다 | 신뢰를 잃는다 | 안전 조건을 단계와 무관하게 고정. 되돌리기 제공. 등록된 일정도 수정·삭제 가능 |
| 학교 PC가 느리다 | 정상 경로의 10초 목표를 넘거나 타임아웃 | 10초는 지원 버전·기준 PC의 성능 목표이고, 각 UI 단계의 실패 판정 타임아웃은 최대 25초다. 실제 단계별 시간을 기록하고 실패해도 다음 예약 몫에서 다시 시도한다 |

---

## 14. 시연 대본 (3분)

1. **문제** — 쿨메신저 쪽지 24건을 보여 준다. "이 안에 일정이 몇 개 있을까요"
2. **지금** — 일곱 단계를 말로 짚는다
3. **바꾼 뒤** — 이미 로그인됐거나 공급사 자동 로그인이 설정된 쿨메신저를 종료해 둔 뒤 앱에서
   단추 하나를 누른다. 쿨메신저가 열리고 내려받은 뒤 **캘린더에 일정이 생긴다**. 시연 전 실측값을
   표시하며 4~5초를 보장값으로 말하지 않는다
4. **자동** — "이건 매일 아침 8시에 저절로 일어납니다"
5. **바탕화면** — 위젯을 보여 준다. "컴퓨터를 켜면 앱을 열지 않아도 여기 있습니다"
6. **경계** — 후보함을 연다. "헷갈리는 건 넣지 않습니다. 요일만 적힌 마감, 변경 안내,
   개인정보가 든 쪽지는 선생님이 확인합니다"

**3번이 이 시연의 전부다.** 여기서 막히면 나머지는 의미가 없으므로,
시연 직전에 반드시 한 번 돌려 보고 자동 로그인 상태를 확인한 뒤 쿨메신저를 **종료 상태**로 두고 시작한다.
