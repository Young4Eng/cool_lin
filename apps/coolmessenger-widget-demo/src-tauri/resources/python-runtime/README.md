이 폴더는 `scripts/prepare-windows-python.ps1` 이 빌드 때 채운다.
Windows embeddable CPython 3.12 x64 와 pillow, numpy, opencv-python 이 들어간다.
바이너리는 커밋하지 않는다. Ollama 와 모델은 넣지 않는다.
CoolMessenger.exe 와 실제 쪽지 xls 도 넣지 않는다.
