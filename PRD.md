# PRD — 쿨메신저 일정도우미

> 최종 검토본 v4 · 2026-08-24
>
> 이 문서는 Claude Code에 전달하여 Windows 설치형 앱을 구현하기 위한 기준 문서다. 문서에 명시되지 않은 기능을 임의로 확대하지 말고, 아래 Release Gate를 순서대로 통과한다.
>
> **이번 구현 범위 고정 (2026-08-27):** 이 앱은 교사 PC에 설치되는 로컬 전용 프로그램이다. R2(휴대폰 PWA·Firebase 서버 동기화)는 이번 구현에서 제외한다. 대상 Gate는 R0(로컬 파이프라인)·R1(위젯)·R3(쿨메신저 자동 다운로드)이며, R2 관련 절은 삭제하지 않고 향후 확장 옵션 참고용으로만 남긴다.

요구 수준 표기:

- **MUST:** 해당 Release Gate 완료에 필수
- **SHOULD:** 전국 공개 전 필수이나 앞 단계에서는 보류 가능
- **COULD:** 선택 확장. 핵심 기능을 지연시키면 구현하지 않음
- 별도 표기가 없으면 해당 절이 속한 Release Gate의 MUST로 간주한다.

## 0. Claude Code 실행 지시

1. 먼저 이 PRD를 읽고 Release Gate별 `IMPLEMENTATION_PLAN.md`를 작성한다.
2. 구현 전 모호하거나 기술적으로 불가능한 요구사항, 개인정보·배포 제약에 걸리는 요구사항만 질문한다. 사소한 UI 결정은 합리적으로 판단한다.
3. **R0만 먼저 구현하고 테스트한다. R0 승인 전에는 모바일 서버, 쿨메신저 UI 자동조작, 전국 배포를 구현하지 않는다.**
4. 원본 메시지와 개인정보를 외부 API·원격 로그·분석 서비스로 전송하지 않는다.
5. 샘플·테스트 데이터에는 실제 교직원명, 학생명, 학번, 전화번호, 계정, 비밀번호를 사용하지 않는다.
6. 각 Release Gate 종료 시 실행 방법, 테스트 결과, 위협 모델, 알려진 제한, 미완료 항목을 `README.md`에 갱신한다.
7. 추정으로 구현하지 말고, 지원하지 않는 플랫폼·파일·메신저 버전은 UI와 README에 명시한다.
8. 다음 Gate로 넘어가기 전 해당 Gate의 인수 테스트 결과를 표로 제출하고 사용자의 승인을 기다린다.

---

## 1. 제품 개요

### 제품명

- 공개 기본명: `교사 일정도우미`
- 프로젝트 작업명: `쿨메신저 일정도우미`
- 공급사 명칭 사용 검토 전에는 제휴·공식 제품으로 오인될 이름과 로고를 사용하지 않는다.

### 한 줄 설명

쿨메신저에서 내려받은 메시지 XLS를 로컬에서 분석하여 잡담과 단순 공지를 제외하고, 교사에게 필요한 일정·마감·할 일 후보만 확인받아 캘린더와 Windows 알림으로 관리하는 설치형 앱.

### 해결하려는 문제

- 학교 업무 일정이 쿨메신저 메시지 속에 섞여 있어 놓치기 쉽다.
- 날짜가 적힌 모든 메시지를 일정으로 등록하면 잡담·과거 일정·참고 공지로 캘린더가 오염된다.
- 교사가 일정을 일일이 다시 입력하는 데 시간이 든다.
- 동일 공지가 여러 번 전달되거나 답장에 이전 대화가 포함되어 중복 알림이 발생한다.
- 메시지 원문에는 학생 개인정보, 건강정보, 전화번호, 계정·비밀번호, 출입 비밀번호가 포함될 수 있다.

### 핵심 가치

1. 필요한 일정만 놓치지 않는다.
2. 자동 판단을 강요하지 않고 교사가 최종 결정한다.
3. 원문을 외부로 보내지 않는 로컬 우선 구조를 사용한다.
4. 설치 후 교사가 별도의 개발 도구를 사용할 필요가 없다.

---

## 2. 대상 사용자

### 주 사용자

- Windows PC에서 쿨메신저를 사용하는 중·고등학교 교사
- 담임, 학년부장, 업무부장 등 메시지와 일정이 많은 교사

### 사용 환경

- Windows 10/11
- 쿨메신저 데스크톱 앱
- Microsoft Excel 설치 여부는 사용자마다 다를 수 있음
- 인터넷이 차단되거나 외부 서비스 사용이 제한된 학교 PC 가능
- 해상도와 디스플레이 배율이 서로 다름

### 전국 배포를 위한 현실적 제품 정의

- 1차 지원 대상은 `Windows + 쿨메신저 XLS` 사용자다.
- “전국 모든 교사”는 장기 목표이며, 모든 학교가 쿨메신저를 사용한다고 가정하지 않는다.
- 제품 내부 명칭은 `교사 일정도우미`, 첫 번째 입력 어댑터 명칭은 `CoolMessenger XLS Adapter`로 분리한다.
- 향후 다른 메신저·업무메일·수동 CSV를 추가할 수 있도록 Source Adapter interface를 사용한다.
- 새로운 입력 소스를 추가해도 분류·후보함·캘린더·알림 UI를 다시 만들지 않는다.

### 교사 숙련도 가정

- 파일 확장자, Python, API, 데이터베이스를 모르는 사용자도 사용한다.
- 설치 권한이 없는 학교 PC 사용자가 존재한다.
- Microsoft Store, Firebase, 외부 웹사이트가 차단된 학교가 존재한다.
- 한글 Windows 사용자 폴더명, OneDrive 문서 폴더, 다중 모니터, 125~150% 화면 배율을 일반 환경으로 간주한다.
- 앱 사용을 위해 Microsoft·Google 계정 가입을 요구하지 않는다.
- Power Automate Desktop, OneDrive, Azure AD 조직 계정에 의존하지 않는다.
- 모바일 PWA 연결에도 Google·Apple·Microsoft 로그인을 요구하지 않는다.

### 핵심 사용성 목표

| 지표 | 목표 |
|---|---|
| 설치 후 첫 일정 후보 확인 | 3분 이내 |
| 반복 사용 시 새 후보 확인 | 10초 이내 |
| 일정 1건 등록 | 2클릭 이내 |
| 잘못 등록한 일정 복구 | 1회 실행 취소 |
| 최초 사용 중 기술 용어 노출 | 0개 |
| 기본 화면의 주요 선택지 | 화면당 5개 이내 |
| 비정상 종료 없는 세션 | 99.5% 이상 |

---

## 3. 제품 원칙

1. **후보 우선:** 확실한 공식 일정·개인 마감 외에는 자동등록하지 않는다.
2. **알림 최소화:** 모든 일정에 D-3·D-2·D-1을 일괄 적용하지 않는다.
3. **로컬 우선:** XLS 파싱, 분류, 저장, 알림은 기본적으로 사용자 PC에서 수행한다.
4. **민감정보 차단:** 비밀번호·계정·학생정보가 포함된 메시지는 일정 후보 생성 전에 보안 격리한다.
5. **설명 가능성:** 후보 카드에 “왜 일정으로 판단했는지” 근거를 표시한다.
6. **사용자 통제:** 등록, 할 일 전환, 무시, 수정, 삭제를 언제든지 할 수 있다.
7. **처음은 작게:** 첫 가져오기에서 전체 기록을 분석하지 않는다.
8. **계정 없는 핵심 기능:** PC 로컬 기능은 회원가입 없이 완전하게 동작한다.
9. **실패해도 업무 지속:** 모바일·네트워크·자동 다운로드가 실패해도 수동 가져오기와 로컬 알림은 유지한다.
10. **업데이트보다 신뢰:** 새 기능보다 오탐 감소, 데이터 보존, 쉬운 복구를 우선한다.

---

## 4. 범위와 Release Gate

### R0 — 로컬 데스크톱 핵심 MVP

R0는 Claude Code의 첫 구현 범위다.

- 사용자가 쿨메신저 XLS/XLSX 파일을 직접 가져오기
- 받은메시지·보낸메시지 시트 파싱
- 답장 인용문 분리, 중복 제거, 변경·취소·반복 일정 후보 처리
- 민감정보 탐지·마스킹
- 메시지 분류 및 일정 후보 생성
- 후보 확인함: `일정 등록 / 할 일 등록 / 무시`
- 안전 조건을 모두 만족하는 후보의 캘린더 자동 등록 (9장 자동등록 조건 기준)
- 오늘·주간·월간 캘린더
- 일정 유형별 Windows 로컬 알림
- 모든 데이터 로컬 SQLite 저장
- 계정 없이 사용 가능
- 최근 14일 기본 분석 및 과거 기록 제외
- 합성 fixture, 단위·통합 테스트, 개발용 실행 절차
- CoolMessenger XLS Source Adapter interface

R0에서 제외: 모바일 서버·PWA, 위젯, 백업 이동, UI 자동조작, Store 제출, 원격 업데이트.

### R1 — 로컬 제품화·교사 파일럿

- 오늘·내일 일정 바탕화면 위젯
- 서명된 Windows 설치파일과 제거·업데이트·롤백
- 앱 내 도움말, 개인정보가 제거된 진단 보고서
- 암호화 백업·복원
- 5명 교사 파일럿과 R0 정확도·사용성 수정
- Windows 사용자별 데이터 격리와 공유 PC 점검

### R2 — 휴대폰 알림 (이번 구현 범위 제외)

> **2026-08-27 결정:** 로컬 전용 구성으로 확정되어 이번 프로젝트에서는 R2를 구현하지 않는다. 아래 내용은 향후 확장 시를 위한 참고 사양으로만 남긴다.

R1이 안정된 뒤 구현하며, 전국 공개 제품의 MUST 범위다.

- 별도 앱스토어 설치 없이 사용할 수 있는 모바일 PWA
- PC 앱에 표시된 일회용 QR 코드로 휴대폰 연결
- Android와 지원되는 iPhone/iPad의 Web Push
- 교사가 승인한 일정의 암호화된 최소 payload와 알림 시각만 서버에 동기화
- PC가 꺼져 있어도 예약된 휴대폰 푸시 발송
- 휴대폰에서 오늘·내일 일정 및 미완료 할 일 확인
- 기기 연결 해제, 키 회전, 서버 데이터 삭제

### R3 — 쿨메신저 다운로드 자동화

**이 제품의 핵심 가치가 여기서 완성된다.** 앞 단계까지는 사용자가 파일을 손으로 내려받아야 하고, 그 한 단계가 남아 있는 한 「매일 쓰는 도구」가 되지 못한다.

자동조작은 **입력 단계만 바꾼다.** 파일을 얻는 방법이 달라질 뿐, 그 뒤의 파싱·마스킹·후보 판정·자동등록 조건은 손으로 가져왔을 때와 완전히 같은 경로를 지난다.

- 쿨메신저 메시지 관리함의 XLS 다운로드 UI 자동조작
- 다운로드 폴더 감시 및 새 파일 자동 가져오기
- **Windows UI Automation 으로 구현한다.** 단추를 이름으로 찾아 누르며, 고정 좌표 클릭은 쓰지 않는다 (창 위치·해상도·배율이 바뀌면 곧바로 틀리기 때문)
- 자동조작 실패 시 중단하고 수동 가져오기 안내. 무한 재시도 금지
- 사용자가 별도의 AutoHotkey 또는 Window Spy를 설치하지 않아도 동작해야 함
- 쿨메신저 화면 구성이 바뀌어 자동조작이 실패해도 **폴더 감시는 그대로 동작한다.** 「내려받기만 손으로」 상태로 자연히 내려앉으며 기능 전체가 죽지 않아야 함
- 사용자가 기능을 켜고 화면을 보며 한 번 실행해 성공한 뒤에 예약을 켜도록 안내
- 기본값은 수동 실행이며, 예약 시각·요일과 수업 중 일시정지를 쉽게 바꿀 수 있음
- 잠금 화면, 전체 화면 발표, 사용자 키보드·마우스 조작 중에는 시작하지 않고 다음 안전 시점으로 연기
- 알려진 쿨메신저 process·publisher·window selector·예상 화면 상태가 모두 맞을 때만 조작하고 계정·비밀번호를 입력하거나 저장하지 않음
- 감시 폴더는 쿨메신저의 기본 저장 위치(바탕화면)와 다운로드 폴더를 기본값으로 하되 사용자가 바꿀 수 있어야 함
- **폴더 안의 모든 파일을 열지 않는다.** 쿨메신저 내보내기의 이름 규칙(`coolmsg_` 접두어)과 확장자가 모두 맞는 파일만 읽는다. 사용자의 성적 파일·명렬표는 열지 않음
- 새 파일은 임시 확장자가 사라지고 크기·수정시각이 안정된 뒤 file signature를 검증해 가져옴
- 첫 설정에서 `성공 후 휴지통으로 이동 / 파일 유지`를 고르게 하고 정확한 폴더와 보존 위험을 설명. 실패 파일은 자동 이동·삭제하지 않음

### R4 — 전국 공개 준비

- 학교 3곳·교사 30명 → 공개 베타 100명 이상의 단계적 검증
- 설치 신뢰, 업데이트, 개인정보 처리방침, 지원·보안 신고 체계
- x64 안정판 우선. ARM64는 실제 빌드·테스트 완료 후 지원 표시
- R0~R2 전체 인수 기준과 공개 기준 통과
- `쿨메신저` 명칭·로고 사용 가능 여부와 비제휴 고지 문구를 공급사 상표 정책 또는 협의로 확인. 확인 전 공개 제품명은 `교사 일정도우미`, 입력 기능명은 `쿨메신저 파일 가져오기`로 제한

### 선택 확장

- 학교별 분류 규칙 공유용 설정 파일 내보내기·가져오기
- ICS 내보내기
- 사용자가 명시적으로 허용한 경우에만 외부 캘린더 연동
- 선택적 온디바이스 모델 또는 사용자 제공 AI API
- 다른 학교 메신저·업무메일·CSV Source Adapter

### R0~R2 제외

- 쿨메신저 DB 직접 접근 또는 역공학
- 원본 메시지 서버 업로드
- 교사 간 중앙 서버 동기화
- 학생·학부모 대상 기능
- 자동 답장 또는 메시지 발송
- Google Calendar·Outlook Calendar 자동 연동
- AI가 사용자 확인 없이 모든 후보를 자동등록하는 기능
- 전국 단위 중앙 관리자 대시보드
- 학교별 사용 현황을 수집하는 강제 텔레메트리
- 공급사 허용·호환성 검증 없는 UI 자동조작 전국 배포
- 서버가 일정 제목이나 본문을 평문으로 읽는 모바일 동기화

---

## 5. 권장 기술 스택

### 데이터 흐름과 신뢰 경계

```text
[사용자 선택 XLS]
        │ 원문은 여기와 Rust 메모리에만 존재
        ▼
[Rust parse → 보안 탐지 → 마스킹 → 분류]
        ├──▶ [로컬 SQLite · R1 사용자 텍스트 암호화] ──▶ [캘린더 / Windows 예약 알림 / R1 위젯]
        │
        └── 사용자 승인 일정만, PC에서 payload 암호화
             ▼
        [Firebase: 발송시각 + ciphertext + subscription]
             │ 일반 제목 Web Push
             ▼
        [R2 PWA: 기기에서 복호화]
```

- 서버는 일정 내용의 신뢰 경계 밖에 있으며 평문 제목을 받지 않는다.
- R3 UI 자동화는 XLS를 얻는 입력 단계만 바꾸고 이후 파이프라인·보안 규칙을 우회하지 않는다.

### 데스크톱 앱

- Tauri 2
- React + TypeScript + Vite
- Rust backend

### 로컬 저장

- SQLite
- SQL 마이그레이션 파일 포함

### XLS 파싱

- Rust `calamine` 사용
- 구형 BIFF `.xls`와 `.xlsx` 모두 지원
- Microsoft Excel 설치에 의존하지 않을 것

### 알림

- 1순위: Windows `ScheduledToastNotification` 또는 현재 공식 동등 API로 OS에 예약하고 tag/group으로 취소·교체
- 패키지 identity가 없는 Win32 빌드는 Microsoft가 안내하는 desktop compatibility 경로를 사용
- Tauri notification plugin의 즉시 발송 기능만으로 예약 알림이 해결된다고 가정하지 않음
- 앱 시작 시 누락된 알림 재계산
- R0 초기에 설치 빌드로 `창 닫힘 / 앱 프로세스 종료 / 재부팅 후 로그인 / 절전 복귀` 스파이크를 먼저 통과
- OS 예약 방식이 목표 설치 패키지에서 불가능할 때만 per-user background scheduler를 대안으로 사용하고, 자동 시작·종료 상태를 사용자에게 명확히 표시

### 모바일 알림

- 모바일 PWA: React + TypeScript + Vite
- Service Worker + 표준 Web Push API
- Firebase Hosting
- Firebase Authentication 익명 인증만 신뢰하지 말고 기기별 서명 capability token, 지원 클라이언트의 App Check, rate limit을 함께 사용
- Firestore: 승인된 일정의 종단간 암호화 payload, 발송에 필요한 시각·무작위 ID, Push Subscription만 저장
- Firebase Functions + Cloud Tasks 또는 동등한 검증된 예약 큐: 예약 알림 생성·취소·발송
- Firebase Secret Manager: VAPID 비밀키 보관
- iPhone/iPad는 iOS/iPadOS 16.4 이상에서 PWA를 홈 화면에 추가한 경우에만 Web Push 지원 안내

### 위젯

- Tauri 별도 window
- frameless, transparent, always-on-top 옵션
- 클릭 통과는 기본 비활성화

### R3 UI 자동화

- **Windows UI Automation 으로 구현한다.** 창을 제목으로 찾고, 그 안의 단추를 이름으로 찾아 누른다
- 단추 이름은 **접두어로** 비교한다. 이름 안에 건수 같은 숫자가 들어가 계속 바뀌는 경우가 있다
- **고정 좌표 클릭과 이미지 인식은 쓰지 않는다.** 창 위치·해상도·배율이 바뀌면 곧바로 틀리고, 왜 틀렸는지 알기도 어렵다
- 별도의 자동화 도구(AutoHotkey, Window Spy 등)를 설치하지 않아도 동작해야 한다

### Windows 배포 기술 제약

- Tauri 2의 기본 Windows 번들은 WiX 기반 MSI와 NSIS setup EXE다. PRD에서 Tauri가 MSIX를 직접 생성한다고 가정하지 않는다.
- **기본안:** 코드 서명된 오프라인 MSI/EXE를 Microsoft Store의 Win32 앱 제출 요건에 맞춰 배포한다.
- 진짜 MSIX가 필요하면 별도의 MSIX packaging pipeline을 기술 검증한 뒤 채택한다. 이를 R0 구현 필수조건으로 두지 않는다.
- Microsoft Store가 MSI/EXE 자체를 대신 코드 서명한다고 가정하지 않는다. 직접 배포 및 Store-linked installer는 신뢰할 수 있는 Authenticode 서명이 필요하다.

### Source Adapter interface

모든 입력 파서는 다음 공통 모델을 반환한다.

```ts
interface MessageSourceAdapter {
  id: string;
  displayName: string;
  canHandle(file: FileDescriptor): Promise<boolean>;
  parse(file: FileDescriptor, options: ParseOptions): AsyncIterable<NormalizedMessage>;
  validate(file: FileDescriptor): Promise<ValidationResult>;
}
```

- Core app은 쿨메신저 시트명·열명·파일명에 직접 의존하지 않는다.
- 어댑터별 파싱 오류와 버전 호환성은 독립적으로 관리한다.
- 사용자는 제품 화면에서 `어댑터`라는 용어를 보지 않고 `쿨메신저 파일`처럼 쉬운 이름만 본다.

---

## 6. 입력 파일 규격

### 기본 가져오기 범위

- 최초 가져오기 기본값은 메시지 발송일 기준 최근 14일이다.
- 미래 일정, 아직 지나지 않은 마감, 오늘 긴급 공지만 후보화한다.
- 과거 완료 일정은 분석 통계에 포함할 수 있지만 후보함에는 넣지 않는다.
- 사용자가 직접 `최근 30일 / 기간 선택 / 전체 기록`으로 확장할 수 있다.
- 전체 기록 선택 시 후보가 많아질 수 있다는 경고와 예상 처리량을 먼저 표시한다.
- 1,000건이 넘는 미확인 후보를 한 번에 만들지 않는다. 안전 상한을 초과하면 기간 축소를 안내한다.

### 필수 시트

- `받은메시지`
- `보낸메시지`

둘 중 하나만 있어도 처리 가능해야 한다.

### 예상 열

- 구분
- 보낸사람 또는 받은사람
- 제목
- 날짜/시간
- 내용
- 첨부파일

### 예외 처리

- 확장자뿐 아니라 file signature를 확인하고, 불일치하면 파싱하지 않는다.
- 기본 안전 한도는 파일 100MB, 시트당 50,000행, 셀당 100,000자로 둔다. 초과 파일은 자동 처리하지 않고 안내하며 실제 샘플 성능에 따라 설정값을 조정한다.
- 열 순서가 달라도 헤더명으로 탐색
- 알 수 없는 열은 무시하되 로그에 기록
- 빈 행, 깨진 행, 보낸사람이 `알 수 없음`인 행을 처리 중단 없이 건너뜀
- OLE 경고가 있더라도 읽을 수 있는 데이터는 처리
- 파일이 열려 있거나 잠겨 있으면 사용자에게 닫고 재시도하도록 안내
- 수식·매크로·OLE 개체·외부 링크를 실행하거나 따라가지 않는다. 셀의 계산된 표시값 또는 안전한 텍스트만 읽는다.
- 파싱은 UI thread 밖에서 시간·메모리 상한과 취소 신호를 적용한다. 제한 초과 시 부분 결과를 확정하지 않는다.
- XLS의 제목·본문·파일명은 신뢰하지 않는 plain text로 취급한다. HTML/Markdown으로 렌더링하거나 `dangerouslySetInnerHTML`을 사용하지 않는다.
- 메시지 안 URL, `file://` 경로, 첨부파일명은 자동으로 열거나 실행하지 않는다. 사용자가 외부 링크를 열 때 실제 host와 경고를 표시한다.
- UI·DB에 넣기 전 제어문자, bidi override, 비정상 초장문을 정규화·제한하고 원문 없이 잘림 여부만 표시한다.

### 원본 XLS 처리

- 앱은 가져온 원본 XLS를 자체 저장공간에 복제하지 않고 사용자가 선택한 파일을 읽기 전용으로 처리한다.
- 원본 파일에 비밀번호·학생정보가 포함될 수 있음을 첫 가져오기 결과에서 1회 안내한다.
- 민감정보가 탐지된 경우 실제 값을 재표시하지 않고 `원본 파일에도 민감정보가 있을 수 있습니다`라는 보안 요약을 표시한다.
- 원본 파일은 앱이 임의로 삭제하지 않는다. `파일 위치 열기 / 보호된 폴더로 이동 / 삭제 안내`를 제공하고, 이동·삭제는 정확한 파일 경로와 복구 가능성을 보여준 뒤 사용자가 확인해야 한다.
- 가져오기 성공 후 파일이 이동·삭제되어도 로컬 캘린더가 깨지지 않아야 한다.
- 가져오기 해시에는 파일 경로나 사용자 이름을 포함하지 않는다.
- R3 앱 관리 폴더의 파일만 사용자가 미리 선택한 정책에 따라 성공 commit 후 휴지통으로 이동할 수 있다. 영구 삭제와 실패 파일 자동 삭제는 금지한다.

---

## 7. 메시지 처리 파이프라인

원문 XLS row는 Rust backend 메모리에서만 처리한다. 보안 탐지·마스킹을 통과하기 전 원문을 React WebView, Tauri event payload, SQLite, clipboard, log, crash report로 보내지 않는다. Frontend에는 마스킹된 DTO와 구조화 후보만 전달한다.

```text
XLS 가져오기
→ 시트·열 검증
→ 메시지 정규화
→ 답장 인용문 분리
→ 중복 제거
→ 민감정보 탐지·마스킹
→ 날짜·시간·장소·담당·행동 추출
→ 메시지 성격 분류
→ 변경·취소·반복·다중 일정 관계 분석
→ 일정 후보 생성
→ 사용자 확인
→ 캘린더·알림 등록
```

### 7.1 정규화

- CRLF와 연속 공백 정리
- 한국어 날짜 표기 통일
- Excel serial date, 문자열 날짜, 날짜/시간 cell type을 지원하고 workbook의 1900/1904 date system 차이를 fixture로 검증한다.
- 정상 학교 메시지 범위를 크게 벗어난 날짜는 후보로 만들지 않고 원본 cell 좌표를 제외한 오류코드만 남긴다.
- `오늘`, `내일`, `모레`, `이번 주 화요일`은 메시지 발송일을 기준으로 절대 날짜 변환
- `5~6교시`, `오후 3시 30분`, `15:20` 지원
- 교시 번호는 전국 공통 시각으로 추정하지 않고 `period_start=5`, `period_end=6`으로 보존한다. 사용자가 학교별 교시표를 로컬 설정한 경우에만 시작·종료 시각으로 변환한다.
- 교시표가 없으면 캘린더에 `5~6교시 · 시간 미설정`으로 표시하고 시작 30분 전 알림은 만들지 않는다. D-1 같은 날짜 기반 알림은 가능하다.
- 날짜만 있는 마감은 `date-only`로 보존하고 임의로 23:59를 넣지 않는다. 기본 당일 알림 시각은 사용자 설정값이며 초기값은 09:00이다.
- 날짜 범위와 입력기간·제출일을 별도 필드로 추출
- 한국 표준시 `Asia/Seoul`을 기본 시간대로 사용한다.
- 학교 일정에서 연도가 생략됐을 때 메시지 발송일과 학사연도 경계를 함께 고려한다.
- 날짜와 요일이 함께 적혔는데 서로 맞지 않으면 자동등록하지 않고 `날짜·요일 불일치` 경고를 표시한다.
- 연도, 오전·오후, 종료일이 모호하면 임의 확정하지 않고 후보에서 사용자의 확인을 요구한다.
- 한 메시지에 서로 다른 날짜·행동이 여러 개 있으면 각각의 후보로 분리하되 동일 원문에서 파생됐음을 묶어서 표시한다.

### 7.1A 사용자 관련성 설정

최초 실행에서 개인정보 없이 다음 로컬 역할 태그만 선택할 수 있다.

- 담임 여부
- 담당 학년
- 보직 여부
- 담당 부서·업무 영역
- 관심 공지: 연수, 평가, 생활지도, 진로, 정보 등

- 선택은 건너뛸 수 있다.
- 이름, 학교명, 학번, 전화번호 입력은 요구하지 않는다.
- 역할 태그는 선택 행사와 타 학년 공지의 관련성 점수에만 사용한다.
- 사용자가 분류 결과에 영향을 준 역할 태그를 언제든지 수정·초기화할 수 있다.

### 7.2 인용문 분리

다음과 같은 패턴 이후는 이전 대화로 저장하되 현재 메시지 판단에서는 제외한다.

```text
OOO님이 보낸글 >>2026/08/24 12:00:00
```

현재 본문이 없고 인용문만 있는 메시지는 일정 후보를 생성하지 않는다.

### 7.3 중복 제거

- 동일 방향, 상대방, 발송시각, 정규화 본문을 해시로 비교
- 동일 공지가 재전송된 경우 제목·본문 유사도와 추출 일정이 같으면 한 후보로 병합
- 병합된 후보에는 원본 메시지 개수와 출처를 표시
- 파일명이 달라도 여러 번 내보낸 XLS의 동일 메시지는 중복 생성하지 않는다.
- 메시지 해시, 추출 일정 fingerprint, 기존 calendar item 관계를 함께 사용하고 단순 제목 일치만으로 병합하지 않는다.

### 7.4 변경·취소·반복·정정

- `취소`, `변경`, `정정`, `연기`, `시간 변경`, `장소 변경` 표현을 새 일정과 구분한다.
- 기존 후보·일정과 높은 신뢰도로 연결되면 `새 일정 추가`가 아니라 `기존 일정 변경/취소 제안`으로 표시한다.
- 연결이 불확실하면 사용자가 대상 일정을 선택하게 하고 자동 삭제·자동 변경하지 않는다.
- 변경 제안에는 이전 값과 새 값을 나란히 표시하고 승인 시 기존 알림을 취소한 뒤 다시 예약한다.
- 취소 제안 승인 시 일정은 즉시 영구 삭제하지 않고 `취소됨` 상태로 보관하며 되돌리기를 제공한다.
- `매주`, `매월`, `매일`, `격주`, `매주 월·수`처럼 규칙이 명시된 경우 반복 후보를 만들 수 있다.
- `당분간`, `수시로`, `매주쯤`처럼 종료·주기가 모호한 반복 표현은 반드시 사용자 확인을 받는다.
- 반복 일정 한 건 수정 시 `이번 일정만 / 이후 일정 / 전체 반복` 범위를 묻는다.

### 7.5 트랜잭션·복구

- 한 Windows 사용자에서 앱은 single-instance로 실행하고 두 번째 실행은 기존 창을 앞으로 가져온다.
- 가져오기 결과는 staging 상태에 쓰고 `messages + candidates + imports`를 원자적으로 commit한다. 취소·오류·crash 시 staging 데이터를 정리한다.
- 후보 승인, calendar item 생성, local reminder 생성, mobile sync queue 추가는 하나의 논리 transaction으로 처리한다.
- `content_fingerprint`, `source_group_id`, reminder idempotency key에는 DB unique constraint를 둔다.
- 앱 시작 시 `staging`에 멈춘 import, orphan reminder, 오래된 sync 작업을 검사하되 사용자 일정은 임의 삭제하지 않는다.
- 시스템 시각·시간대 변경과 절전 복귀를 감지하면 미래 알림만 재계산하고 이미 지난 알림은 `놓친 일정` 정책을 따른다.

---

## 8. 개인정보·보안 요구사항

### 자동 탐지 대상

- 학생 이름과 학번 조합
- 5자리 학번·학생식별번호 패턴
- 전화번호
- 이메일
- 주민등록번호 형태
- 건강·상담·학교폭력 관련 민감 키워드
- `비밀번호`, `비번`, `암호`, `인증번호`, `2차 인증`
- 계정과 비밀번호가 함께 등장하는 메시지
- 출입문·교무실·학년부실 번호

### 처리 정책

- 비밀번호·인증정보: 즉시 `[보안정보 제거]`로 치환하고 일정 후보에서 완전히 제외
- 학생 개인정보·건강·상담정보: 원문·이름을 후보함에 노출하지 않고 `민감 메시지`로 격리
- 격리 메시지에 명확한 미래 날짜·행동이 있으면 실제 값 없이 `학생 관련 일정 가능성 · 8월 27일`처럼 metadata-only 안내와 `일반 제목으로 PC 일정 만들기`를 제공할 수 있다. 기본 제목은 `학생 관련 일정`, 모바일 동기화는 OFF다.
- metadata-only 일정 만들기는 원본·학생 이름·건강 키워드를 title, notes, reasoning, 로그에 복사하지 않고 교사가 날짜와 일반 제목을 직접 확인해야 한다.
- 전화번호·이메일: 화면과 로그에서 마스킹
- 원본 본문과 원본 XLS 사본은 R0~R4 앱 DB·백업에 저장하지 않는다. `원문 로컬 보관` 설정도 제공하지 않는다.
- 분류 근거에는 민감값이 제거된 최소 문장 또는 추출된 구조화 필드만 저장한다.
- 로그에 원문, 이름, 전화번호, 계정, 비밀번호를 기록하지 않는다.
- 크래시 리포팅과 원격 분석은 기본 비활성화
- 개발·테스트용 fixture는 합성 데이터만 사용
- credential로 탐지된 임시 문자열은 처리 직후 가능한 범위에서 메모리에서 지우고 장기 cache에 넣지 않는다. OS crash dump까지 완전히 통제한다고 과장하지 않는다.
- 로컬 DB·설정·키는 Windows 사용자 프로필별로 분리한다. 다른 Windows 계정에서 일정이 보이면 안 된다.
- PC 한 대를 여러 교사가 공유하는 환경에서는 각자의 Windows 계정 사용을 권장하고, 공용 계정 사용 시 앱 잠금과 알림 제목 숨김을 안내한다.
- R1 교사 파일럿 전, 사용자가 작성한 일정 제목·장소·메모와 마스킹 요약은 DPAPI로 보호한 앱 키를 이용해 필드 단위 AEAD 암호화한다. 날짜·상태처럼 캘린더 동작에 필요한 비민감 메타데이터만 평문 저장한다.
- 암호화 전용 키와 dedupe용 keyed hash 키를 분리한다. 복호화 실패 시 DB를 초기화하지 않고 백업·복구 안내를 제공한다.

### 모바일 종단간 암호화와 데이터 최소화

휴대폰 알림을 켜도 서버는 일정 제목·메모를 평문으로 읽을 수 없어야 한다.

서버가 평문으로 저장할 수 있는 값:

- 무작위 계정·기기·일정·알림 식별자
- 정확한 알림 발송 시각과 취소·만료 상태
- 암호화 payload 버전, nonce, ciphertext
- Push Subscription과 발송 재시도 상태

앱 수준 종단간 암호화 payload에만 넣는 값:

- 사용자가 최종 승인·수정한 일정 제목
- 시작·종료·마감 시각, 일정 유형, 완료 여부
- 장소·메모가 필요하면 사용자가 모바일 동기화를 명시적으로 켠 필드만 포함

- 암호화 직전 제목·장소·메모를 다시 민감정보 검사한다. 학생정보·건강·상담·비밀번호 가능성이 있으면 모바일 동기화를 차단하고 `PC에만 보관 / 일반 제목으로 바꾸기`를 제안한다.
- 종단간 암호화는 민감정보를 서버로 보내도 된다는 허가가 아니다.

키 관리:

- PC에서 content-encryption key를 생성하고 Windows DPAPI 또는 Credential Manager로 보호한다.
- QR URL의 fragment 등 서버로 전송되지 않는 경로로 휴대폰에 1회 전달하고, 휴대폰은 IndexedDB에 non-extractable key로 저장한다.
- pairing capability token과 암호화 키를 분리한다. token은 1회용·5분 만료이며 재사용을 거부한다.
- 기기 연결 해제 시 capability를 폐기하고 해당 기기용 키를 회전할 수 있어야 한다.
- 키를 잃으면 서버가 복호화해 주지 않는다. 사용자는 PC에서 기기를 다시 연결한다.

다음 데이터는 서버로 전송하면 안 된다.

- 원본 XLS 파일
- 원본 메시지 본문
- 발신자·수신자 실명
- 첨부파일
- 학생 이름·학번·건강·상담 정보
- 전화번호·이메일
- 계정·비밀번호·인증정보
- 분류기의 전체 디버그 로그

잠금화면 Web Push 본문은 기본적으로 `확인할 학교 일정이 있습니다`처럼 일반 문구만 표시한다. 사용자가 명시적으로 상세 알림을 켠 경우에만 기기에서 ciphertext를 복호화해 제목을 표시하며 서버는 제목을 알 수 없다.

### 서버 위치·보존 원칙

- 모바일 알림을 위한 Firestore, Cloud Functions, 예약 큐는 배포 전 지원 여부를 확인하고 가능한 리소스를 서울 리전 `asia-northeast3`에 고정한다.
- Firestore 위치는 생성 후 변경할 수 없으므로 최초 배포 전에 리전을 확정한다.
- 완료·삭제된 모바일 일정은 30일 이내 자동 삭제한다.
- 연결 해제된 Push Subscription은 즉시 삭제한다.
- 서버 백업에도 원본 메시지·실명·학생정보가 들어가지 않아야 한다.
- 공개 배포 전 개인정보 처리방침, 데이터 삭제 방법, 문의 채널을 앱과 PWA에서 확인할 수 있어야 한다.
- 예약 발송 구현은 안정적인 작업 ID를 사용한다. 일정 수정 시 기존 작업을 취소하고 새 작업을 만들며, 같은 알림 ID의 중복 발송을 idempotency key로 차단한다.
- 예약 큐의 최대 예약 기간·재시도·보존 한계는 배포 시점의 공식 문서와 부하 테스트로 확인하고 README에 기록한다. 한계를 넘는 장기 일정은 일일 materializer가 발송 가능 구간에 들어온 알림만 큐에 넣는다.

### R2 공개 전 개인정보·법무 Gate

기술적 암호화만으로 법적 준수를 단정하지 않는다. 운영 주체가 정해진 뒤 개인정보보호 담당자 또는 전문가가 다음을 확인하기 전에는 모바일 기능을 공개하지 않는다.

- 누가 개인정보처리자이고 Firebase·Push 사업자가 어떤 수탁자·제3자 지위인지
- 처리 목적, 법적 근거, 최소 수집 항목, 보유기간, 파기 절차
- 국내 리전 설정과 별개로 지원·로그·백업·Push 경로에 국외 처리가 있는지와 필요한 고지·동의·계약
- 개인정보 처리방침, 처리위탁·재위탁 공개, 이용자 열람·정정·삭제·처리정지 요청 창구
- 침해사고 대응, 접근권한 검토, 관리자 MFA, 비밀키 회전, 감사 로그 보존기간
- 학교 또는 교육청 보안정책에서 개인 PC 앱과 외부 Push 서비스를 허용하는지

R4 직전에는 배포 시점의 개인정보보호위원회 `개인정보의 안전성 확보조치 기준 안내서`와 처리방침 작성지침 최신본을 다시 확인하고 점검 증적을 `PRIVACY_RELEASE_CHECKLIST.md`에 남긴다.

### 애플리케이션 권한·공급망

- R0 빌드는 외부 네트워크 endpoint와 HTTP client 권한을 포함하지 않는다. R1 업데이트, R2 모바일, R3 manifest는 각 기능을 켤 때만 도메인 allowlist를 추가한다.
- Tauri capability는 최소권한으로 나누고 임의 shell 실행, 임의 파일 읽기, 원격 웹 콘텐츠 로딩을 허용하지 않는다.
- 파일 접근은 사용자가 고른 XLS/XLSX와 앱 전용 데이터·백업 경로로 scope를 제한한다.
- CSP에서 `connect-src`, `script-src`, `frame-src`를 최소화하고 `unsafe-eval`과 임의 원격 script를 금지한다.
- 외부 도움말 링크는 앱 WebView 안에서 로그인·파일 접근 권한과 함께 열지 않고 기본 브라우저로 연다.
- R3 sidecar는 앱에 포함해 서명·hash를 검증하고, 사용자 입력으로 임의 실행 경로·인자를 만들지 않는다.
- 의존성 lockfile, 라이선스 목록, SBOM을 릴리스마다 생성하고 알려진 critical 취약점이 있으면 공개를 차단한다.
- updater·parser rule·kill switch manifest는 서로 구분된 서명 키와 버전·만료 시각·rollback 방지 값을 검증한다.

### 긴급 보안 경고

계정과 비밀번호가 함께 탐지되면 다음 안내만 표시한다.

> 메시지에서 계정 또는 비밀번호로 보이는 정보가 발견되었습니다. 일정으로 저장하지 않았습니다. 현재 사용 중인 정보라면 변경을 권장합니다.

실제 값은 화면에 재표시하지 않는다.

---

## 9. 메시지 분류 모델

### 분류값

| 코드 | 의미 | 기본 처리 |
|---|---|---|
| `OFFICIAL_EVENT` | 필수 공식 행사·회의·연수 | 일정 후보 |
| `PERSONAL_TASK` | 사용자가 해야 할 제출·입력·확인·회신 | 할 일 후보 |
| `DEADLINE` | 제출일·신청마감·작성자마감 | 마감 후보 |
| `OPTIONAL_EVENT` | 희망자·신청자·참석자 대상 | 확인 필요 |
| `REFERENCE_NOTICE` | 참고용 공지·기간 안내 | 낮은 우선순위 또는 제외 |
| `URGENT_NOTICE` | 당일 폭염·안전·시간표 변경 | 즉시 알림, 캘린더 제외 가능 |
| `PERSONAL_CHAT` | 감사·축하·응원·개인 대화 | 제외 |
| `ACK_REPLY` | 네·넵·확인했습니다 등의 단순 답장 | 제외 |
| `SENSITIVE` | 개인정보·비밀번호·건강·상담 내용 | 격리·제외 |
| `UNKNOWN` | 판단 불가 | 사용자 확인 |

### 판정 순서와 점수

한 메시지를 하나의 label로 먼저 고정하지 않는다. 문장을 일정 단위로 분리한 뒤 각 단위에 다음 순서를 적용한다.

1. 민감정보·보안정보면 `SENSITIVE`로 차단
2. 현재 본문이 단순 확인·인사·인용뿐이면 `ACK_REPLY` 또는 `PERSONAL_CHAT`
3. 변경·취소·정정이면 기존 일정 관계 후보
4. 제출·입력·회신 같은 사용자 행동과 기한이 있으면 `PERSONAL_TASK` 또는 `DEADLINE`
5. 의무 표현, 대상, 날짜가 명확한 학교 행사는 `OFFICIAL_EVENT`
6. 희망·신청·선택 표현이 있으면 `OPTIONAL_EVENT`
7. 날짜는 있으나 사용자 행동·참석 의무가 없으면 `REFERENCE_NOTICE`
8. 근거가 충돌하거나 부족하면 `UNKNOWN`

점수 구성 요소:

- 날짜·시간 구체성
- 제출·참석·준비·회신 등 행동 구체성
- `필수`, `전 교직원`, `담임`, 담당 학년 등 의무·관련성
- 선택 표현과 제3자 개인 일정 여부
- 날짜·요일·본문 간 충돌
- 민감정보 탐지 결과

- 규칙 점수 0~1을 통계적으로 보정된 확률처럼 표현하지 않는다. UI는 `높음 / 검토 필요 / 낮음`만 보여준다.
- 초기 기준은 `높음 ≥ 0.90`, `검토 필요 0.60~0.89`, `낮음 < 0.60`이되 golden set으로 조정한다.
- 사용자 피드백 가중치는 한 번의 잘못된 선택으로 전체 분류가 뒤집히지 않도록 상한을 둔다.

### 자동등록 조건

아래 **안전 조건을 모두** 만족하는 후보만 자동으로 캘린더에 등록한다. 하나라도 걸리면 등록하지 않고 후보함에 남겨 사용자 확인을 받는다.

- 날짜가 미래 또는 오늘
- 공식 일정 또는 사용자 마감이 명확함
- 선택 행사 표현이 없음
- 민감정보 없음
- 모호 표시(연도 생략·오전오후 불명확·요일만 적힘·종료일 불명확 등)가 하나도 없음
- 변경·취소 제안이 아님
- 일정 대상이 사용자와 관련 있다고 판단됨

등록 기준은 두 단계이며 사용자가 고른다. **어느 단계에서도 위 안전 조건은 움직이지 않는다.** 단계가 바꾸는 것은 신뢰도 문턱 하나뿐이다.

| 단계 | 기준 |
|---|---|
| `아주 확실한 것만` | 위 조건 + 신뢰도 0.90 이상 |
| **`분명한 일정까지`** (기본값) | 위 조건 |

`아주 확실한 것만`은 실제 쪽지에서 거의 발동하지 않는다. 본문에 `전 교직원 필수` 같은 표현이 있어야 0.90을 넘기 때문이다. 그래서 기본값을 `분명한 일정까지`로 둔다.

자동 등록된 일정도 **사용자가 수정·삭제할 수 있어야 하며, 직전 등록은 되돌리기로 취소할 수 있어야 한다.** 자동 등록 자체를 끄는 스위치를 설정 화면에 둔다.

앱이 스스로 등록한다는 사실과 그 조건은 사용 안내와 참여 동의서에 그대로 적는다.

### 선택 행사 표현

- 희망하시는
- 관심 있으신
- 신청자
- 참석하실 분
- 참여를 원하시는
- 부담 갖지 마시기

이 표현이 있으면 자동등록하지 않고 확인 질문을 보여준다.

### 개인 잡담 판정 예시

- “감사합니다”, “화이팅”, “좋은 주말 보내세요”
- 다른 사람의 개인 출장·개인 연습
- 축하 인사 권유
- 단순 문의와 답변

날짜가 포함돼도 사용자에게 행동 의무가 없으면 일정으로 등록하지 않는다.

---

## 10. 일정 후보 확인함

### 후보 카드 필드

- 추천 제목
- 분류
- 날짜·시간 또는 마감일
- 장소
- 할 일
- 대상
- 출처 메시지 날짜
- 판단 근거
- 신뢰도
- 민감정보 제거 여부
- 관계 상태: 새 일정, 변경 제안, 취소 제안, 반복 일정
- 연결된 기존 일정과 변경 전·후 값

### 버튼

- `일정 등록`
- `할 일 등록`
- `내용 수정`
- `무시`

### 무시 학습

- 사용자의 선택을 로컬 규칙으로 기록
- 동일 발신 역할·유사 제목·동일 키워드의 향후 후보 점수 조정
- 사용자가 학습 기록을 초기화할 수 있어야 함
- 학습 데이터도 외부로 전송하지 않음
- `무시`를 3회 이상 반복한 유사 유형은 이후 기본 접힘 상태로 제안하되 완전 자동 삭제하지 않는다.
- `항상 등록` 규칙은 사용자가 명시적으로 만든 경우에만 적용한다.

### 대량 후보 처리

- 같은 공지에서 파생된 후보를 묶어 한 번에 검토한다.
- `확실한 공식 일정 모두 등록`은 필터 결과가 20건 이하일 때만 제공한다.
- `모두 무시`는 실행 전 대상 수를 표시하고 10초 동안 실행 취소할 수 있다.
- 불명확 후보는 일괄 등록할 수 없다.

---

## 11. 캘린더 요구사항

### 보기

- 월간
- 주간
- 오늘
- 일정 후보함
- 완료된 할 일

### 색상

- 공식 일정: 파랑
- 개인 업무·마감: 주황
- 선택 일정: 보라
- 당일 긴급: 빨강
- 완료: 회색

### 일정 상세

- 제목, 날짜, 시간, 장소, 준비물, 메모
- 출처에는 앱이 저장한 마스킹 요약만 표시한다. 원본 파일이 그대로 있을 때만 사용자가 명시적으로 `원본 파일 위치 열기`를 선택할 수 있다.
- 완료, 수정, 삭제, 알림 변경
- 반복 규칙과 예외 날짜
- 변경·취소 이력에는 마스킹된 변경 요약만 표시
- 취소된 일정은 기본 캘린더에서 흐리게 표시하고 사용자가 숨길 수 있다.

---

## 12. 알림 정책

모든 일정에 D-3·D-2·D-1을 일괄 적용하지 않는다.

| 유형 | 기본 알림 |
|---|---|
| 제출·입력 마감 | D-3, D-1, 당일 오전 |
| 필수 행사·연수 | D-1, 시작 30분 전 |
| 준비물이 있는 일정 | D-2, D-1 |
| 선택 행사 | 등록한 경우에만 D-1 |
| 기간 일정 | 시작 전날 1회 |
| 당일 긴급 공지 | 즉시 1회 |
| 단순 참고 일정 | 알림 없음 |

### 추가 규칙

- 이미 지난 알림은 몰아서 보내지 않는다.
- PC가 꺼져 있던 동안 지난 일정은 앱 실행 시 `놓친 일정` 목록으로만 표시한다.
- 동일 일정의 중복 알림을 금지한다.
- 사용자는 일정별 알림을 변경하거나 끌 수 있다.
- 업무 마감은 공식 제출일과 내부 완료 권장일을 분리할 수 있다.
- 일정 등록 시 이미 D-3 또는 D-2가 지났다면 **현재 시각 이후 알림만** 만든다. 지나간 D-day 알림을 즉시 보내지 않는다.
- 기본 방해금지 시간은 22:00~07:00이며 사용자가 변경·해제할 수 있다. 긴급 공지도 기본값에서는 방해금지 시간을 무시하지 않는다.
- 토·일요일과 방학에 대한 전역 알림 정책을 제공하되 일정별 설정이 우선한다.
- `10분 / 30분 / 1시간 / 내일 오전` 미루기를 제공하고, 같은 알림의 반복 미루기 횟수를 제한할 수 있다.
- Windows 잠금화면·Toast 개인정보 보호 모드를 기본 ON으로 하고 제목 대신 `확인할 학교 일정이 있습니다`를 표시한다. 사용자가 상세 제목 표시를 명시적으로 켤 수 있다.
- OS 예약 알림에는 reminder ID를 tag, 앱 범주를 group으로 넣어 수정·취소 시 정확히 제거한다.
- Windows 예약 알림의 짧은 전달 허용시간을 벗어나 PC가 꺼져 있었다면 OS가 버린 알림을 다시 Toast로 쏟지 않고 앱의 `놓친 일정`에만 표시한다.
- 알림을 눌러 앱이 시작될 때 해당 일정으로 deep-link되고, 일정이 삭제·잠금 상태면 안전한 오늘 화면을 연다.
- background scheduler fallback을 쓸 경우 창 닫기와 `완전히 종료`를 구분하고, 완전히 종료하면 알림이 제한될 수 있음을 종료 전에 안내한다.

### 12.1 휴대폰 푸시 정책

- 휴대폰 알림은 사용자 명시적 동의 후에만 활성화한다.
- 기본값은 Windows 알림 ON, 모바일 알림 OFF다.
- 일정별로 `PC만 / 휴대폰만 / 둘 다 / 알림 없음`을 선택할 수 있다.
- PC와 휴대폰에 동시에 보내는 경우 같은 알림 ID를 사용해 중복 상태를 관리한다.
- 모바일 알림을 눌렀을 때 해당 일정 상세 화면을 연다.
- 완료된 할 일의 예약 알림은 즉시 취소한다.
- 일정 시각이 변경되면 기존 예약을 취소하고 새 예약을 만든다.
- 네트워크 실패 시 PC 앱은 동기화 대기열에 저장하고 다음 연결 시 재시도한다.
- 서버는 발송 완료 후 불필요한 알림 payload를 삭제한다.
- 잠금화면에서 민감한 제목을 숨기는 `개인정보 보호 모드`를 기본 ON으로 한다.
- 서버 예약 작업은 `(device_id, reminder_id, remind_at)` idempotency key를 사용한다.
- 수정·완료·삭제 동작은 기존 예약 취소 성공 여부와 무관하게 최신 상태 버전을 증가시키며, 오래된 작업은 발송 직전 상태 버전을 확인하고 종료한다.
- 서버 `remind_at`은 UTC instant로 저장하고 표시·입력만 `Asia/Seoul`로 변환한다.
- Web Push는 OS·브라우저·절전 정책에 따라 지연될 수 있는 best-effort 보조 알림이다. `반드시 정시에 도착`한다고 표현하지 않고 PC 캘린더를 기준 기록으로 유지한다.
- 서버에는 내용 없는 `scheduled / dispatched / provider_rejected / expired` 상태와 시각만 기록하고, 기기별 마지막 알림 테스트 결과를 사용자가 볼 수 있게 한다.

---

## 12A. 휴대폰 연결과 모바일 화면 (R2)

### 최초 연결

1. PC 앱에서 `휴대폰 알림 연결` 선택
2. 서버에서 5분 동안만 유효한 일회용 pairing token 발급
3. PC 앱에 QR 코드 표시
4. 휴대폰 카메라로 QR 코드 스캔
5. PWA에서 기기 이름 확인 및 알림 권한 요청
6. Android는 `홈 화면에 추가` 안내
7. iPhone은 Safari에서 `공유 → 홈 화면에 추가` 안내 후 설치된 PWA에서 알림 권한 요청
8. PC 앱과 휴대폰 양쪽에 연결 성공 표시

### 모바일 화면

- 오늘
- 내일
- 이번 주
- 미완료 할 일
- 일정 상세
- 완료 체크
- 알림 미루기: 10분, 30분, 1시간
- 알림 설정
- 기기 연결 해제
- R2 PWA는 일정 제목·날짜·반복 규칙을 만들거나 수정하지 않는다. PC가 기준 기록이며 휴대폰은 완료 체크와 해당 기기 알림 미루기만 보낸다.
- 첫 연결 후 app shell과 암호화 일정 cache를 사용해 일시적 오프라인에서도 오늘·내일을 표시한다.
- 오프라인 완료 체크는 로컬 queue에 저장하고 재연결 후 state version 충돌을 검사해 동기화한다.
- 완료는 단조 상태로 병합하고 PC에서 명시적으로 `다시 열기`한 더 높은 state version만 완료를 되돌릴 수 있다. 미루기는 선택한 기기에만 적용한다.

### 연결 보안

- QR pairing token은 1회용이며 5분 후 만료
- token 원문을 로그에 남기지 않음
- 한 사용자가 연결할 수 있는 기기 수 기본 3대
- PC에서 개별 기기의 마지막 접속 시각 확인 및 강제 연결 해제 가능
- 연결 해제 시 해당 Push Subscription과 예약 알림 삭제
- 90일 이상 미접속 기기는 자동 만료 후보로 표시
- QR에는 장기 인증정보를 평문 query string으로 넣지 않는다. 암호화 키는 URL fragment 또는 동등한 서버 비가시 채널로 전달한다.
- 휴대폰 알림 권한은 사용자가 `알림 켜기`를 누른 직접 상호작용 안에서만 요청한다.
- 각 기기는 독립 capability와 키를 가지며 한 기기 해제가 다른 기기를 끊지 않는다.
- R2를 처음 켤 때 PC가 기기 서명 key pair를 생성한다. private key는 DPAPI/Credential Manager로 보호하고 public key만 서버에 등록한다.
- 일정 생성·수정·취소 요청은 nonce, timestamp, body hash를 PC private key로 서명한다. 서버는 재사용 nonce와 허용 시간 밖 요청을 거부한다.
- 휴대폰은 QR 교환 후 자신의 범위로 제한된 capability를 받는다. PC 일정 전체 수정, 다른 기기 관리, 새 기기 발급 권한을 기본 부여하지 않는다.
- 이메일·전화번호 기반 계정 복구는 제공하지 않는다. PC 분실 시 연결된 PWA에서 `모든 서버 데이터 삭제`를 실행할 수 있어야 한다.
- 인증된 PC·휴대폰이 180일 동안 한 번도 접속하지 않으면 서버 일정·subscription을 자동 파기한다. 시행 전 30일과 7일에 연결된 기기로 일반 문구 알림을 보낸다.
- PWA의 Cache Storage, URL, analytics, console log에는 복호화된 제목·장소·메모를 남기지 않는다. IndexedDB에는 ciphertext와 non-extractable key만 저장하고 화면 표시 시 메모리에서 복호화한다.
- Service Worker 업데이트와 push handler 실패 시 일반 제목 알림으로 축소하며 ciphertext나 key를 오류 로그에 넣지 않는다.
- 연결 해제된 PWA는 다음 온라인 접속에서 capability 거부를 확인한 즉시 local ciphertext·key·subscription을 지운다. 현재 기기에서 해제하면 즉시 지운다.
- 공유 휴대폰이나 화면 잠금이 없는 기기에는 연결하지 말라는 안내를 제공한다. 앱 자체 암호화가 분실된 잠금 해제 기기의 화면 접근까지 막는다고 홍보하지 않는다.
- Firestore는 기본 deny로 두고 클라이언트의 임의 collection write를 허용하지 않는다. 모든 변경은 서명·capability·schema·크기·state version을 검증하는 Functions API를 통한다.
- PWA capability는 자신의 owner ciphertext 읽기와 자신의 완료·미루기만 허용한다. 다른 owner ID를 바꿔 넣는 요청은 emulator 통합 테스트에서 거부한다.
- CORS origin은 운영 PWA domain으로 제한하고 관리자·Functions service account에는 최소 IAM 권한과 MFA를 적용한다.

---

## 13. 바탕화면 위젯 (R1)

### 보기 전환 (2026-08-27 확정)

위젯은 **창 하나**에서 `목록` 보기와 `달력` 보기를 전환한다. 두 보기를 위해 별도 창을 만들지 않는다.

- `목록` 보기: 오늘 일정, 내일 일정, 미완료 마감 3건 이내
- `달력` 보기: 축소된 월간 달력. 위젯 크기 범위(260~420 × 56~560) 안에서 표시하고, 필요하면 내부 스크롤을 허용한다.
- 두 보기 모두에서 일정 상세 편집은 메인 앱을 열어서 한다.

### 기능

- 투명도 60~90% 조절
- 항상 위 켜기·끄기
- 화면 모서리 스냅
- 접기·펼치기
- 목록 · 달력 보기 전환
- 완료 체크
- 민감한 제목 숨김 모드
- 마지막 위치와 크기 저장

### 기본값

- 항상 위: OFF
- 투명도: 82%
- 위젯 자동 시작: OFF
- 개인정보 보호 모드: ON

메인 앱의 전체 캘린더 창과 별개로, 위젯은 목록·달력 두 보기를 하나의 작은 창 안에서 전환하며 유지한다.

---

## 14. 주요 사용자 흐름

### 최초 실행

1. 앱 실행
2. 개인정보·로컬 처리 원칙 안내
3. XLS 파일 선택
4. 파싱 진행률 표시
5. 처리 결과 요약
6. 일정 후보함 표시
7. 후보 확인
8. 캘린더와 알림 활성화 여부 선택

최초 실행 간소화 규칙:

- PC 로컬 사용에는 계정 생성, 휴대폰 연결, 역할 설정을 필수로 요구하지 않는다.
- 첫 파일을 고르기 전 긴 개인정보 약관 전체를 강제로 읽게 하지 않는다. 핵심 3줄 요약과 상세 보기로 분리한다.
- 기본 추천값으로 진행할 수 있는 `빠른 시작`을 Primary Action으로 제공한다.
- 최초 성공 후에만 휴대폰 연결과 세부 설정을 제안한다.
- 모바일 연결은 R2의 선택 설정이며 첫 로컬 일정 등록 성공을 막지 않는다.

### 반복 사용

1. R0/R1은 새 XLS 직접 가져오기, R3은 선택적으로 다운로드 폴더 감지
2. 이미 처리한 메시지 제외
3. 새 후보 수 알림
4. 후보 확인
5. 캘린더 반영

### 잘못 분류된 경우

1. 후보 카드에서 `무시` 또는 분류 변경
2. 유사 메시지 처리 규칙 저장 여부 확인
3. 다음 가져오기부터 반영

### 변경·취소 공지를 받은 경우

1. 새 XLS 가져오기
2. 기존 일정과 연결된 `변경 제안` 또는 `취소 제안` 표시
3. 사용자가 변경 전·후 내용을 비교
4. 승인하면 기존 일정 상태와 알림을 갱신하고 실행 취소 제공
5. 연결할 기존 일정을 찾지 못하면 새 일정으로 자동 생성하지 않고 선택 요청

---

## 15. 데이터 모델

아래 이름은 논리 필드다. R1부터 일정 제목·장소·메모·마스킹 요약처럼 사람이 읽을 수 있는 텍스트의 실제 열은 `*_ciphertext`, `*_nonce`, `key_version` 형태로 저장하고 평문 열을 migration에서 제거한다. 정렬·조회에 필요한 날짜, 유형, 상태, 무작위 ID는 평문일 수 있다. 중복 비교는 원문 hash가 아니라 별도 키로 만든 HMAC fingerprint를 사용한다.

### `imports`

- id
- file_name
- file_hash
- imported_at
- source_modified_at
- received_count
- sent_count
- skipped_count
- status: staging, committed, failed, cancelled
- error_code nullable

### `messages`

- id
- import_id
- direction
- counterpart_masked
- title_masked
- sent_at
- normalized_body_masked
- attachment_name_masked
- content_fingerprint: keyed HMAC, unique within normalized source scope
- security_flags_json
- classification
- confidence
- created_at

### `candidates`

- id
- message_id
- proposed_title
- candidate_type
- start_at
- end_at
- due_at
- time_precision: date_only, period, exact
- period_start nullable
- period_end nullable
- location
- action_text
- target_text
- reasoning
- confidence
- relation_type: new, update, cancel, recurrence
- supersedes_candidate_id nullable
- linked_calendar_item_id nullable
- recurrence_rule nullable
- recurrence_exceptions_json nullable
- ambiguity_flags_json
- source_group_id
- status: pending, accepted_event, accepted_task, applied_update, applied_cancel, ignored
- created_at
- reviewed_at

### `calendar_items`

- id
- candidate_id nullable
- title
- item_type
- start_at
- end_at
- due_at
- location
- notes
- time_precision: date_only, period, exact
- period_start nullable
- period_end nullable
- status: active, completed, cancelled
- series_id nullable
- recurrence_rule nullable
- recurrence_exceptions_json nullable
- source_group_id nullable
- completed_at nullable
- color
- created_at
- updated_at
- state_version

### `reminders`

- id
- calendar_item_id
- remind_at
- reminder_type
- state_version
- idempotency_key
- server_task_id nullable
- fired_at nullable
- dismissed_at nullable

`reminders.idempotency_key`는 unique이며 calendar item 변경 transaction에서 state version과 함께 증가한다.

### `preferences`

- key
- value_json

### `classification_feedback`

- id
- message_pattern_hash
- original_classification
- user_action
- created_at

### `mobile_devices`

- id
- local_user_id
- device_name
- server_device_id
- paired_at
- last_seen_at
- notification_enabled
- privacy_mode
- key_version
- capability_id
- revoked_at nullable

### `sync_queue`

- id
- entity_type
- entity_id
- operation: upsert, delete, cancel_reminders
- payload_minimized_json
- state_version
- attempts
- next_retry_at
- synced_at nullable

### 서버 `mobile_schedules`

- owner_id
- device_scope
- schedule_id
- payload_version
- nonce
- ciphertext
- state_version
- updated_at

### 서버 `mobile_reminders`

- owner_id
- device_id
- schedule_id
- reminder_id
- remind_at
- state_version
- idempotency_key
- task_id nullable
- status: scheduled, cancelled, sent, expired

### 서버 `push_subscriptions`

- owner_id
- device_id
- endpoint_encrypted 또는 provider token
- keys_encrypted
- created_at
- last_seen_at
- revoked_at nullable

### 서버 `owners`

- owner_id: 무작위 식별자
- pc_public_key
- capability_version
- last_authenticated_at
- purge_after

이메일, 전화번호, 이름, 학교명은 owner 식별에 사용하지 않는다.

서버 데이터에는 원본 message_id, 메시지 본문, 교직원 실명, 학생정보를 저장하지 않는다.
서버 데이터에는 일정 제목·장소·메모도 평문으로 저장하지 않는다.

### 로컬 키·백업 메타데이터

- 암호화 키 자체를 SQLite 평문 열에 저장하지 않는다.
- 현재 Windows 사용자용 키는 DPAPI 또는 Credential Manager로 보호한다.
- 다른 PC로 옮길 백업은 DPAPI만으로 암호화하면 복원할 수 없으므로, 사용자가 만든 복구 암호 또는 별도 recovery key로 암호화한다.
- 복구 암호를 잃으면 개발자도 복원할 수 없다는 사실을 내보내기 전에 알린다.

---

## 16. 오류 처리

- 지원하지 않는 파일: “쿨메신저 XLS/XLSX 파일인지 확인해 주세요.”
- 시트 없음: 발견한 시트명을 표시하고 재선택 안내
- 파일 잠김: Excel을 닫고 재시도 안내
- 일부 행 오류: 전체 중단 없이 건너뛰고 건수만 표시
- 알림 권한 없음: 설정 이동 안내
- 모바일 연결 실패: 일회용 QR을 폐기하고 새 QR 생성 안내
- 휴대폰 푸시 미지원: OS·브라우저 요건과 홈 화면 추가 절차 안내
- 모바일 서버 장애: 로컬 캘린더와 Windows 알림은 정상 유지
- 모바일 암호화 키 분실: 서버 복구를 시도하지 않고 PC에서 기기 재연결 안내
- 예약 작업 취소 실패: 상태 버전을 폐기 처리하여 오래된 작업의 발송을 차단하고 백그라운드 정리 재시도
- SQLite 손상: 백업 후 복구 시도, 실패 시 새 DB 생성 여부 질문
- UI 자동화 실패: 수동 XLS 가져오기 버튼 표시
- 쿨메신저 화면 버전 미지원: 자동조작을 즉시 중지하고 해당 버전·오류코드·수동 가져오기 안내
- 날짜·요일 불일치: 둘 중 하나를 임의 선택하지 않고 후보 수정 화면으로 이동
- 변경·취소 대상 불명확: 기존 일정을 사용자에게 선택하게 하며 새 일정으로 자동등록하지 않음
- 학교 방화벽 차단: 차단된 기능만 표시하고 로컬 모드로 계속 사용
- 업데이트 실패: 현재 버전 유지, 다음 시작 시 재시도
- 새 버전 파서 오류: 이전 파서 규칙으로 롤백
- 진단 보고서 생성: 원문 없이 앱 버전·OS·어댑터·오류코드·건수만 내보내기
- 고객지원 문구는 오류코드와 `복사` 버튼을 제공하고 개발자 스택트레이스를 직접 노출하지 않는다.

---

## 17. 성능 기준

- 5,000건 메시지 XLS를 일반 학교 PC에서 10초 이내 1차 처리 목표
- UI는 파싱 중 멈추지 않아야 함
- 동일 파일 재가져오기 시 해시로 즉시 중복 감지
- 앱 유휴 메모리 목표 200MB 이하
- WebView2 온라인 bootstrapper를 쓰는 일반 설치파일은 80MB 이하 목표. Store·폐쇄망용 offline WebView2 포함 패키지는 별도 크기 목표를 정하고 다운로드 크기를 사전에 표시한다.
- 첫 화면 표시 2초 이내 목표
- 3,000건 가져오기 중에도 취소 버튼이 작동해야 함
- 저사양 기준 테스트 환경을 README에 명시

### 품질·신뢰 목표

- 공식 일정 Precision 목표 95% 이상
- 민감정보 탐지 Recall 목표 99% 이상. 탐지 의심 시 안전하게 후보 제외
- 같은 일정 중복 생성률 1% 미만
- 사용자가 앱을 하루 동안 사용하며 받는 기본 알림 5건 이하 목표
- 후보 중 `잡담인데 일정으로 제안됨` 비율 3% 미만 목표
- 목표 미달 시 자동등록 기능을 활성화하지 않는다.

### 평가 데이터셋

- 실제 원문을 중앙 수집하지 않고 합성·비식별 fixture로 golden test set을 만든다.
- 학교급, 학사연도 경계, 교시 표현, 줄임말, 변경·취소·반복, 개인정보 패턴이 서로 다른 사례를 포함한다.
- Precision·Recall의 분모, 정답 라벨 규칙, 제외 기준을 `EVALUATION.md`에 고정한다.
- 파서·규칙 변경 시 golden set 전체 회귀 테스트를 실행하고 기준 미달이면 배포를 차단한다.
- 교사 파일럿 피드백은 원문이 아니라 `제안 결과 + 교사가 고친 구조화 필드`를 본인 동의 후 비식별 집계한다.

---

## 18. 접근성·사용성

- 기본 글꼴 14px 이상
- 주요 버튼은 한글 텍스트 포함
- 색상만으로 상태를 구분하지 않음
- 초보 사용자를 위한 단계별 최초 실행 안내
- 기술 용어 대신 `일정 후보`, `할 일`, `무시` 사용
- 삭제 전 확인, 되돌리기 제공
- Windows 화면 배율 100%, 125%, 150%, 175%에서 검증
- 1366×768부터 4K, 다중 모니터에서 검증
- 고대비 모드와 Windows 텍스트 크기 확대 대응
- 날짜·시간은 한국어로 표시하되 내부 저장은 표준 형식 사용
- 단축키를 몰라도 모든 기능 사용 가능

---

## 18A. UI·UX 디자인 명세

### 참고 기준

- UI 요소 명칭과 용도는 [NameThatUI](https://namethatui.com/)의 시각 사전을 참고한다.
- 시각 스타일은 메인 앱에 **Minimalism**, 바탕화면 위젯에만 절제된 **Glassmorphism**을 적용한다.
- Neumorphism, Neobrutalism, Masonry, Bento Grid, Carousel은 이 제품의 업무 가독성과 맞지 않으므로 사용하지 않는다.
- 예쁜 화면보다 `후보 확인 → 결정 → 일정 확인`을 적은 시선 이동과 클릭으로 끝내는 것을 우선한다.

### 18A.1 정보구조

데스크톱 앱은 고정된 왼쪽 Sidebar와 한 개의 주 콘텐츠 영역으로 구성한다.

```text
오늘
일정 후보       [5]
캘린더
할 일
휴대폰 연결
설정
```

- `일정 후보`의 미확인 개수만 Badge로 표시한다.
- `휴대폰 연결` 메뉴는 R2에서만 표시한다. R0/R1에 비활성 `준비 중` 메뉴를 노출하지 않는다.
- Sidebar는 기본 224px, 아이콘과 한글 레이블을 함께 사용한다.
- 창 폭이 좁아지면 아이콘 Sidebar로 축소하되 Tooltip으로 레이블을 제공한다.
- 데스크톱 기본 화면에서 Hamburger Menu를 사용하지 않는다.
- 상단 Header에는 현재 화면 제목, 마지막 가져오기 시각, `메시지 파일 가져오기` Primary Button만 둔다.
- 좌측 메뉴 항목은 전국 사용자 테스트 전까지 6개를 넘기지 않는다.
- 도움말은 설정 깊숙한 곳이 아니라 Header의 `?` 버튼과 각 Empty State에서 접근한다.

### 18A.2 최초 실행 — Steps

R0/R1 최초 실행은 다음 3단계 Steps indicator를 사용한다.

1. 파일 가져오기
2. 분석 결과 확인
3. 알림 설정

- 현재 단계 하나에만 `aria-current="step"`을 적용한다.
- 완료 단계는 숫자 대신 Check icon을 표시한다.
- 미래 단계는 muted color로 표시한다.
- 단순 진행률은 Progress Bar, 이름 있는 단계는 Steps를 사용하여 혼용하지 않는다.
- R2에서는 첫 로컬 성공 완료 화면에 `휴대폰도 연결하기` Secondary Action을 제공한다. 별도의 연결 흐름이며 3단계 완료 상태를 바꾸지 않는다.

### 18A.3 오늘 화면

앱 시작 시 기본 화면은 월간 달력이 아니라 `오늘`이다.

상단 요약:

- 오늘 일정 수
- 미완료 할 일 수
- 새 일정 후보 수

본문 순서:

1. 다음 일정
2. 오늘 시간순 일정
3. 마감 임박 할 일
4. 새 일정 후보가 있으면 확인 Callout

- 한 화면에 통계 Card를 3개 넘게 배치하지 않는다.
- 일정이 없으면 “오늘 등록된 일정이 없습니다” Empty State와 `캘린더 보기` 버튼을 제공한다.
- 오늘 화면에 월간 달력 전체를 넣지 않는다.

### 18A.4 일정 후보함 — Split View

데스크톱에서는 가장 중요한 화면이므로 Master–Detail Split View로 구현한다.

- 왼쪽 38%: 후보 목록
- 오른쪽 62%: 선택된 후보의 상세와 결정 버튼
- 가운데 Divider는 드래그로 너비를 조절할 수 있다.
- 창이 좁으면 목록만 표시하고 후보 선택 시 오른쪽 Drawer로 상세를 연다.

후보 목록 행:

- 분류 Chip
- 추천 제목
- 추출 날짜·시간
- 판단 근거 한 줄
- 신뢰도는 숫자 대신 `높음 / 검토 필요 / 낮음` 레이블
- 메시지 본문은 2줄 Line Clamp 후 Ellipsis

오른쪽 상세:

- 추천 제목 Form Field
- 날짜·시간 Date Picker
- 장소
- 할 일 또는 준비물
- “왜 이 후보가 생겼나요?” Accordion
- 마스킹 상태 Inline Alert
- 알림 방식 Segmented Control
- 하단 고정 Action Bar

하단 Action Bar 버튼 순서:

```text
[무시]        [할 일로 등록] [일정 등록]
```

- `일정 등록`을 Primary Button으로 한다.
- `무시`는 파괴적 삭제가 아니므로 빨간색을 쓰지 않는다.
- 버튼은 스크롤 중에도 보이도록 오른쪽 pane 하단에 Sticky 처리한다.
- 한 후보를 처리하면 다음 미확인 후보를 자동 선택한다.
- 처리 직후 5초간 Toast에 `실행 취소`를 제공한다.
- 전국 배포 기본값은 `새 메시지부터`이며 과거 수천 건 후보를 첫 화면에 쌓지 않는다.
- 필터 기본값은 `미확인 + 오늘 이후`다.

### 18A.5 Candidate Card 구조

모바일·좁은 화면에서는 후보를 Card로 표시한다.

- Card Header: 분류 Chip + 제목
- Metadata Row: 날짜·시간·장소
- Card Body: 판단 근거와 추출 내용
- Card Footer: `무시 / 할 일 / 일정`
- Footer는 `margin-top:auto`로 하단에 고정하여 Card 높이가 달라도 버튼 위치가 흔들리지 않게 한다.
- Card 안에 또 다른 Card를 중첩하지 않는다.

### 18A.6 캘린더

상단에 `오늘 / 주간 / 월간` Segmented Control을 둔다.

- 세 보기는 같은 캘린더 데이터의 배타적 표시 방식이므로 Tabs보다 Segmented Control을 사용한다.
- `후보 / 등록 일정 / 완료`처럼 서로 다른 콘텐츠 영역 전환에는 Tabs를 사용한다.
- 오늘 날짜는 Outline, 선택한 날짜는 Solid Fill로 명확하게 구분한다.
- 기간 일정은 시작일·종료일과 중간 구간을 연속된 Range Highlight로 표시한다.
- 날짜만 있는 값은 `yyyy-mm-dd` civil date로 저장하여 UTC 변환으로 하루가 어긋나지 않게 한다.
- 일정이 많은 날은 셀 안에 3건까지만 표시하고 `+2건` Badge로 상세 Popover를 연다.
- 일정 생성·수정에는 Date Picker를 사용하며 키보드 입력도 허용한다.

### 18A.7 알림 UI

용도에 따라 알림 UI를 구분한다.

| UI 요소 | 사용 위치 |
|---|---|
| Banner | 앱 전체에 영향을 주는 동기화·업데이트 장애 |
| Inline Alert | 민감정보 발견, 알림 권한 없음, 후보별 주의 |
| Callout | 최초 사용법·기능 설명 |
| Toast | 등록·수정·무시 완료와 실행 취소 |
| Modal Dialog | 영구 삭제, 전체 데이터 초기화처럼 반드시 결정해야 하는 작업 |

- 일반 성공 메시지에 Modal을 사용하지 않는다.
- 긴급하지 않은 동적 안내는 `role="status"`, 즉시 대응이 필요한 오류만 `role="alert"`를 사용한다.
- 색상뿐 아니라 Icon과 텍스트로 severity를 함께 표현한다.
- Toast는 스스로 사라지되 키보드 포커스를 강제로 가져가지 않는다.

### 18A.8 Badge·Chip·Tag 규칙

- Badge: 개수 또는 상태 수치. 예: 미확인 후보 `5`
- Chip: 사용자가 인식·선택할 수 있는 분류. 예: `공식 일정`, `마감`, `선택 행사`
- Tag: 검색·필터용 메타데이터. 예: `1학년`, `연수`, `상담`
- Pill 모양은 장식 목적으로 남용하지 않는다.
- Chip 색상은 분류 색상과 일치하되 텍스트 레이블을 반드시 포함한다.

### 18A.9 Switch·Checkbox·Radio 규칙

- Switch: 켜는 즉시 적용되는 독립 설정. 예: `바탕화면 위젯 항상 위`
- Checkbox: 여러 항목을 동시에 선택. 예: 표시할 일정 유형
- Radio: 하나만 선택. 예: 기본 알림 대상 `PC / 휴대폰 / 둘 다`
- 저장 버튼을 눌러야 적용되는 설정에 Switch를 사용하지 않는다.

### 18A.10 가져오기 진행 상태

- Empty State 전체를 Drop Zone으로 만들되 동일 위치에 `쿨메신저 파일 선택` Primary Button을 둔다. 드래그를 모르는 사용자도 완전히 사용할 수 있어야 한다.
- File Picker는 `.xls`, `.xlsx`를 우선 표시하고 마지막으로 성공한 폴더는 현재 Windows 사용자 로컬 설정에만 저장한다.
- `파일을 어디서 받나요?` 도움말은 쿨메신저 버전별 짧은 그림 안내와 `다운로드 폴더 열기`를 제공한다. 앱이 사용자의 전체 디스크를 자동 검색하지 않는다.
- 실제 파일 없이 볼 수 있는 `예시로 둘러보기`는 합성 메시지만 사용하고 예시 데이터임을 계속 표시한다.
- 단계별 처리량을 알 수 있으면 Progress Bar와 `2,150 / 3,275건 분석 중` 텍스트를 표시한다.
- 정확한 진행률을 알 수 없는 짧은 작업만 Spinner를 사용한다.
- 결과 목록 레이아웃이 정해져 있고 로딩이 400ms 이상이면 Skeleton을 사용한다.
- 가져오기 완료 후 Toast와 함께 `새 후보 7건 · 제외 3,120건 · 보안 격리 4건` 요약을 표시한다.
- 파싱 도중 화면 전체를 막는 Modal을 사용하지 않는다.

### 18A.11 휴대폰 PWA (R2)

하단 Navigation Bar는 4개로 제한한다.

```text
오늘 | 이번 주 | 할 일 | 설정
```

- 모바일 첫 화면도 `오늘`이다.
- 일정 상세는 Bottom Sheet로 연다.
- 알림 미루기 옵션은 Action Sheet 형태로 제공한다.
- 완료 체크는 최소 44×44px 터치 영역을 확보한다.
- QR 연결 자체는 `QR 만들기 → 휴대폰에서 열기 → 알림 테스트 → 완료`의 4단계 Steps와 큰 단일 Primary Button을 사용한다.
- iPhone 홈 화면 추가 안내는 닫을 수 없는 영구 Modal이 아니라 단계별 Callout으로 제공한다.
- 알림 권한을 페이지 진입 즉시 요청하지 않고 사용자가 `휴대폰 알림 켜기`를 누른 뒤 요청한다.
- PWA 설치 실패 시 일정 자체가 사라진 것처럼 보이지 않게 PC 로컬 사용 상태를 명확히 안내한다.

### 18A.12 바탕화면 위젯 시각 기준 (R1)

- Main app과 달리 약한 Glassmorphism 허용: 반투명 표면, 배경 blur, 1px 밝은 edge
- 텍스트가 있는 영역의 실효 배경 불투명도는 최소 78%를 유지한다.
- 배경 사진과 관계없이 본문 대비율 4.5:1 이상을 확보한다.
- 위젯 그림자는 한 단계만 사용하고 과한 glow·gradient를 금지한다.
- `목록` 보기(오늘·내일·임박 마감)와 축소된 `달력` 보기를 같은 창에서 전환한다(13장 참고). 창 크기 범위(260~420 × 56~560) 안에서 표시하며 필요하면 내부 스크롤을 허용한다.
- 상세 확인·편집이 필요하면 메인 앱의 오늘 화면으로 연결한다.

### 18A.13 시각 토큰

```text
Font: Pretendard 우선, 없으면 Segoe UI, Malgun Gothic
Body: 14px / 1.5
Small metadata: 12px / 1.4
Page title: 24px / 1.25, 700
Section title: 18px / 1.35, 700
Radius: Card 12px, Input 8px, Modal 16px
Spacing scale: 4, 8, 12, 16, 24, 32
Control height: 40px desktop, 44px mobile
Content max width: 1280px
```

색상 의미:

- Primary: 차분한 Indigo 또는 Blue
- Official event: Blue
- Personal task/deadline: Amber
- Optional event: Violet
- Urgent: Red
- Completed: Neutral Gray
- Success: Green

- 순백색·순검정 대신 눈부심이 적은 neutral surface를 사용한다.
- Light mode를 먼저 완성하고 Dark mode는 MVP 이후로 둔다.
- 배경 gradient는 로그인·빈 상태 illustration 외에는 사용하지 않는다.

### 18A.14 금지 UI 패턴

- 중요한 버튼을 Three Dots Overflow Menu 안에 숨기지 않는다.
- 같은 화면에 Primary Button을 두 개 이상 두지 않는다.
- 아이콘만 있는 주요 버튼을 사용하지 않는다.
- 후보를 Carousel로 넘기게 하지 않는다.
- 일정 후보를 Masonry 또는 Pinterest Grid로 배치하지 않는다.
- 통계 중심 Bento Dashboard를 첫 화면으로 사용하지 않는다.
- 모든 내용을 Modal로 띄우지 않는다.
- 본문을 Hover해야만 볼 수 있게 하지 않는다.
- 자동으로 움직이는 Marquee, Parallax, Text Scramble을 사용하지 않는다.
- 장식 애니메이션은 200ms 이하의 opacity·translate 전환으로 제한하고 `prefers-reduced-motion`을 지원한다.

---

## 19. 인수 테스트

### 파싱

- [ ] 받은메시지·보낸메시지 양쪽 시트를 읽는다.
- [ ] 3,000건 이상 파일을 중단 없이 처리한다.
- [ ] 인용 답장의 이전 본문으로 후보를 만들지 않는다.
- [ ] 같은 파일을 두 번 가져와도 후보가 중복되지 않는다.
- [ ] `8월 24일`, `8/24`, `2026.8.24`, `오늘`, `내일`을 날짜로 변환한다.
- [ ] Excel serial date와 1900/1904 workbook fixture의 날짜가 기대값과 일치한다.
- [ ] `15:20`, `오후 3시 20분`, `5~6교시`를 처리한다.
- [ ] 학교 교시표가 없을 때 `5~6교시`를 임의 시각으로 바꾸지 않고, 교시표 설정 후에만 정확한 시각 알림을 만든다.
- [ ] 날짜만 있는 마감에 23:59를 임의 저장하지 않는다.
- [ ] 서로 다른 내보내기 파일에 포함된 동일 메시지를 한 번만 처리한다.
- [ ] 한 메시지의 서로 다른 일정·마감을 각각 후보로 만들고 source group으로 묶는다.
- [ ] 날짜와 요일이 불일치하면 자동등록하지 않고 경고한다.
- [ ] 연도·오전/오후가 불명확하면 임의 확정하지 않는다.
- [ ] 확장자와 file signature가 다른 파일, 안전 한도 초과 파일을 실행·부분 저장 없이 거부한다.
- [ ] XLS의 수식·매크로·OLE·외부 링크가 실행되지 않는다.
- [ ] `<script>`, bidi 제어문자, `file://`, 초장문이 포함된 셀을 가져와도 script·링크·파일이 실행되지 않고 UI가 깨지지 않는다.
- [ ] 가져오기 취소·crash 후 반쪽 import와 후보가 확정 상태로 남지 않는다.

### 분류

- [ ] “감사합니다” 단독 메시지는 후보에서 제외한다.
- [ ] 다른 사람의 개인 출장은 사용자의 일정으로 등록하지 않는다.
- [ ] 필수 연수는 공식 일정 후보가 된다.
- [ ] 제출 마감은 할 일·마감 후보가 된다.
- [ ] 희망자 대상 설명회는 자동등록하지 않는다.
- [ ] 당일 폭염 공지는 즉시 공지로 처리하고 장기 캘린더를 오염시키지 않는다.
- [ ] `시간 변경`, `장소 변경`, `취소`, `정정` 공지는 기존 일정 변경·취소 제안이 된다.
- [ ] 변경 대상을 찾지 못한 공지는 새 일정으로 자동등록하지 않는다.
- [ ] 명확한 매주 반복은 반복 후보가 되고 모호한 반복은 확인을 요구한다.

### 보안

- [ ] 비밀번호 값이 UI·DB·로그에 남지 않는다.
- [ ] 마스킹 전 원문이 WebView IPC/event, clipboard, crash report payload에 포함되지 않는다.
- [ ] 전화번호와 이메일이 마스킹된다.
- [ ] 학생 건강정보·이름 원문은 후보·캘린더·로그에 노출되지 않고, 선택적 metadata-only 일정에도 실제 값이 복사되지 않는다.
- [ ] 네트워크 연결 없이 핵심 기능이 동작한다.
- [ ] 앱이 원격 분석 요청을 보내지 않는다.
- [ ] 선택한 원본 XLS를 앱 데이터 폴더에 복제하지 않는다.
- [ ] 다른 Windows 사용자 계정에서 기존 사용자의 로컬 일정과 키를 볼 수 없다.
- [ ] 진단·crash fixture와 golden set에 실제 사용자 원문이 포함되지 않는다.
- [ ] R1: 일정 제목·장소·메모가 SQLite 파일에서 평문 검색되지 않고 DPAPI 보호 키 없이는 복호화되지 않는다.
- [ ] R0 실행 중 DNS/HTTP 요청이 없고 Tauri capability에 임의 shell·광범위 파일 읽기 권한이 없다.
- [ ] updater·rule·manifest의 서명·만료·rollback 검증 실패 시 적용하지 않고 기존 안전 버전을 유지한다.

### 알림·캘린더

- [ ] 마감 일정은 D-3, D-1, 당일 알림을 생성한다.
- [ ] 공식 행사는 D-1과 시작 30분 전 알림을 생성한다.
- [ ] 참고 일정은 기본 알림이 없다.
- [ ] 이미 지난 알림을 앱 실행 시 연속 발송하지 않는다.
- [ ] D-1에 등록한 마감은 지나간 D-3 알림을 발송하지 않는다.
- [ ] 일정 변경 시 기존 예약을 취소하고 최신 알림만 남긴다.
- [ ] 취소 승인과 실행 취소가 캘린더·알림에 일관되게 반영된다.
- [ ] 반복 일정 수정 범위 `이번만 / 이후 / 전체`가 정확히 적용된다.
- [ ] 방해금지 시간에는 일반 알림이 표시되지 않고 다음 허용 시각 정책을 따른다.
- [ ] 설치 빌드에서 창과 앱 프로세스를 닫은 뒤 예약 시각에 알림이 도착하거나, 승인된 background fallback이 동작한다.
- [ ] 알림을 누르면 해당 일정 상세로 열리고 삭제된 일정이면 오류 없이 오늘 화면으로 이동한다.
- [ ] PC를 예약 시각보다 5분 이상 껐다 켜도 지난 Toast가 연속 발송되지 않고 `놓친 일정`에만 표시된다.
- [ ] 일정 수정·취소 후 같은 tag/group의 과거 Windows 예약 알림이 남지 않는다.
- [ ] R1: 위젯 투명도와 항상 위 설정이 재실행 후 유지된다.

### 휴대폰 알림

- [ ] 일회용 QR로 휴대폰을 연결할 수 있다.
- [ ] 만료되거나 재사용된 QR token을 거부한다.
- [ ] 승인한 일정의 최소 데이터만 서버로 전송한다.
- [ ] 원본 메시지·발신자명·학생정보·비밀번호는 요청 payload에 포함되지 않는다.
- [ ] Firestore와 Functions 로그·관리자 화면에서 일정 제목·장소·메모를 평문으로 읽을 수 없다.
- [ ] 사용자가 일정 제목에 학생정보·건강·상담·비밀번호를 입력하면 모바일 동기화를 차단한다.
- [ ] QR의 암호화 키가 서버 access log와 query parameter에 남지 않는다.
- [ ] PC가 꺼져 있어도 예약된 테스트 푸시가 휴대폰에 도착한다.
- [ ] 완료한 할 일의 남은 푸시가 취소된다.
- [ ] 일정 수정 시 기존 알림이 취소되고 새 시각으로 재예약된다.
- [ ] 모바일 개인정보 보호 모드에서 잠금화면에 일정 제목이 노출되지 않는다.
- [ ] Android PWA 설치·알림 절차가 동작한다.
- [ ] iPhone 지원 요건과 홈 화면 추가 절차가 화면에 명확히 표시된다.
- [ ] PC에서 기기 연결을 해제하면 서버의 Push Subscription과 예약 알림이 삭제된다.
- [ ] 오래된 예약 작업이 재시도돼도 최신 state version이 아니면 푸시를 보내지 않는다.
- [ ] 동일 idempotency key의 작업을 두 번 실행해도 푸시는 한 번만 도착한다.
- [ ] 휴대폰 오프라인 완료와 PC 일정 수정이 충돌해도 최신 제목·시각을 잃지 않고 완료 상태만 안전하게 병합한다.
- [ ] 한 휴대폰의 미루기가 다른 휴대폰과 PC의 알림 시각을 바꾸지 않는다.
- [ ] 키 분실 시 일정 내용을 서버에서 복구하지 않고 안전하게 재연결한다.
- [ ] PWA Cache Storage·IndexedDB·console·URL에 복호화된 제목·장소·메모가 남지 않는다.
- [ ] 기기 해제 후 해당 PWA가 다시 온라인이 되면 local ciphertext·key·subscription을 삭제한다.
- [ ] owner/device ID를 다른 값으로 바꾼 API·Firestore 요청이 모두 거부되고 cross-tenant ciphertext도 반환되지 않는다.

### UI·UX

- [ ] 로컬 최초 실행이 3단계 Steps로 표시되고 현재 단계 하나에만 `aria-current="step"`이 있다.
- [ ] 휴대폰 연결은 첫 일정 등록을 막지 않으며 별도 선택 흐름이다.
- [ ] 앱 시작 시 월간 달력이 아니라 오늘 화면이 열린다.
- [ ] 후보함이 데스크톱 Split View, 좁은 화면 Drawer로 동작한다.
- [ ] 후보 등록·무시 후 다음 후보가 자동 선택되고 Toast에서 실행 취소할 수 있다.
- [ ] 중요 정보가 색상만으로 구분되지 않는다.
- [ ] Date Picker에서 날짜가 UTC 변환으로 하루 어긋나지 않는다.
- [ ] Badge, Chip, Switch, Checkbox, Radio가 명세된 용도대로 사용된다.
- [ ] 빈 화면마다 설명과 다음 행동이 있는 Empty State가 표시된다.
- [ ] 마우스 드래그 없이 File Picker만으로 가져올 수 있고 `예시로 둘러보기`가 실제 데이터와 섞이지 않는다.
- [ ] 바탕화면 위젯의 텍스트 대비율이 배경과 무관하게 4.5:1 이상이다.
- [ ] 주요 데스크톱 Control은 40px, 모바일 Touch target은 44×44px 이상이다.
- [ ] 키보드만으로 Sidebar, 후보 결정, 캘린더, 설정을 사용할 수 있다.
- [ ] `prefers-reduced-motion`에서 불필요한 애니메이션이 제거된다.
- [ ] 바탕화면 위젯이 하나의 창 안에서 목록·달력 보기를 전환하며 별도 창을 만들지 않는다.

### 설치

- [ ] AutoHotkey, Window Spy, Python, Excel을 별도 설치하지 않은 Windows PC에서 R0~R2가 동작한다.
- [ ] 일반 사용자 권한으로 설치·실행 가능하다.
- [ ] 대화형 제거는 앱 파일을 제거하고 사용자 데이터 보존 여부를 묻고, silent uninstall은 관리자 가이드의 보존·삭제 옵션을 따른다.
- [ ] 설치파일의 서명자와 해시를 확인할 수 있고 SmartScreen 경고 상태를 파일럿에서 기록한다.
- [ ] DB migration 실패 시 자동 백업에서 복원하고 이전 앱 버전으로 실행된다.
- [ ] `모두 삭제` 후 Windows 예약 알림, 서버 task·subscription, 로컬 DB·키가 남지 않고 별도 백업·원본 XLS는 삭제하지 않는다.

### R3 자동 다운로드

- [ ] 공급사 공개 문서·이용조건·협의 중 최소 하나로 자동조작 배포 가능성을 확인하고 기록한다.
- [ ] 지원하는 쿨메신저 버전·화면 배율 목록을 명시한다.
- [ ] selector를 찾지 못하면 클릭을 시도하지 않고 수동 가져오기로 전환한다.
- [ ] 같은 XLS가 다시 다운로드되어도 후보가 중복되지 않는다.
- [ ] 별도의 AutoHotkey와 Window Spy 설치 없이 동작한다.
- [ ] kill switch가 자동화를 끄더라도 수동 가져오기와 로컬 일정은 정상 동작한다.
- [ ] 잠금 화면·전체 화면 발표·키보드 또는 마우스 사용 중 자동화가 시작되지 않는다.
- [ ] 예상 process·publisher·selector 중 하나라도 다르면 입력·클릭하지 않는다.
- [ ] `.tmp`, `.crdownload`, 크기가 변하는 파일을 가져오지 않는다.
- [ ] 성공 commit된 앱 관리 폴더 파일만 선택 정책에 따라 휴지통으로 이동하고 실패 파일은 보존한다.

### 전국 환경 호환성

- [ ] 한글 Windows 사용자명과 OneDrive 문서 경로에서 동작한다.
- [ ] Windows 10/11 x64에서 동작하고 ARM64 지원 여부를 명확히 표시한다.
- [ ] 100%, 125%, 150%, 175% 화면 배율에서 주요 버튼이 잘리지 않는다.
- [ ] 1366×768 화면에서 후보 결정 버튼이 스크롤 없이 접근 가능하다.
- [ ] Microsoft Store·Firebase가 차단돼도 로컬 파일 가져오기와 Windows 알림이 동작한다.
- [ ] 역할 설정을 건너뛰어도 핵심 기능이 동작한다.
- [ ] 첫 가져오기 기본 범위가 최근 14일이며 과거 일정이 후보함을 채우지 않는다.
- [ ] 진단 보고서에 원문·실명·전화번호·비밀번호가 포함되지 않는다.
- [ ] 업데이트 실패 후 기존 버전으로 정상 실행된다.

---

## 20. Release Gate 완료 정의

### R0 완료

1. 개발 빌드에서 합성 XLS/XLSX를 가져올 수 있다.
2. 일정 후보·할 일·잡담·민감 메시지·변경·취소·반복을 구분한다.
3. 사용자가 후보를 일정 또는 할 일로 등록하고 수정·무시·되돌릴 수 있다.
4. 오늘·주간·월간 캘린더와 Windows 알림이 오프라인에서 작동한다.
5. R0 인수 테스트와 golden set 회귀 테스트가 통과한다.
6. 원문과 민감정보가 외부로 전송되지 않는다.

### R1 완료

1. 비개발자가 서명된 설치파일로 설치·제거할 수 있다.
2. 투명 위젯, 진단, 암호화 백업·복원, 업데이트 롤백이 작동한다.
3. 새 PC 복원용 recovery key/암호 경로를 실제로 검증한다.
4. 교사 5명 중 90% 이상이 계정 없이 3분 안에 첫 후보를 확인한다.

### R2 완료 — 휴대폰 포함 제품 MVP

1. QR로 연결한 지원 Android와 iPhone/iPad에서 모바일 푸시를 받는다.
2. PC가 꺼져 있어도 서버에 예약된 알림이 도착한다.
3. 서버는 일정 제목·장소·메모·원문을 평문으로 알 수 없다.
4. 일정 수정·완료·삭제·기기 해제 후 오래된 푸시가 발송되지 않는다.
5. 학교 네트워크가 모바일 서버를 차단해도 R0/R1 로컬 기능이 유지된다.
6. 비개발자가 따라 할 수 있는 PC 설치·휴대폰 연결 안내가 있다.

### R3 완료

1. 공급사 호환성·허용 근거와 지원 버전을 문서화한다.
2. 지원 환경에서 자동 다운로드 성공률 95% 이상을 달성한다.
3. 실패 시 오작동 없이 수동 가져오기로 복귀한다.

### R4 완료

20B의 공개 기준, 설치 신뢰, 운영·보안·지원 준비를 모두 통과한다.

---

## 20A. 전국 배포·업데이트·지원

### 배포 경로

1. **일반 사용자 기본:** Microsoft Store에 제출한 코드 서명된 Tauri Win32 오프라인 installer(MSI 또는 EXE). Store 정책과 제출 테스트를 통과한 형식을 사용
2. **Store 차단·설치권한 없음:** 현재 Windows 사용자 범위로 설치되는 Authenticode 서명 NSIS EXE. 온라인/오프라인 WebView2 변형과 용량을 명확히 구분
3. **학교 관리자 배포:** 서명된 MSI를 우선으로 조용한 설치·제거 명령, 시스템 범위 정책, 배포 가이드 제공

- 일반 공개에 무서명 EXE 또는 자체서명 인증서를 사용하지 않는다.
- Tauri가 MSIX를 직접 생성한다고 가정하지 않는다. 별도 MSIX package를 도입할 때만 Store의 MSIX 서명 절차를 적용한다.
- MSI/EXE는 Microsoft Store가 대신 서명해 주지 않는다는 전제로 게시자 코드 서명을 준비한다.
- 다운로드 페이지에 버전, 게시일, SHA-256, 변경사항, 지원 OS를 표시한다.
- 설치파일이 변조되지 않았는지 검증할 수 있어야 한다.
- 베타와 안정 버전을 분리하고 교사 기본값은 안정 버전이다.

### 업데이트

- 보안 업데이트와 일반 기능 업데이트를 구분한다.
- 자동 업데이트는 수업 중 강제 재시작하지 않는다.
- 데이터베이스 마이그레이션 전 자동 백업한다.
- 업데이트 실패 시 이전 버전으로 롤백한다.
- 파서 규칙은 앱 버전과 독립적으로 버전 관리하되 서명 검증 후 적용한다.
- 릴리스 노트는 개발 용어 대신 `무엇이 달라졌는지` 5줄 이내로 제공한다.

### 백업·이동

- 사용자가 로컬 일정·설정·분류 학습을 암호화된 백업파일로 내보낼 수 있다.
- 새 PC에서 백업파일을 가져올 수 있다.
- 새 PC용 백업은 현재 PC의 DPAPI 키에만 의존하지 않고 복구 암호 또는 별도 recovery key로 암호화한다.
- 복구 암호는 서버에 보내지 않으며 강도 확인, 재입력, 안전한 보관 안내를 제공한다.
- 백업파일에는 원본 XLS와 원문 메시지를 포함하지 않는다.
- 백업·복원 실패 시 기존 데이터를 덮어쓰지 않는다.

### 데이터 삭제

- 설정에서 `PC 데이터만 삭제 / 휴대폰 연결·서버 데이터만 삭제 / 모두 삭제`를 분리해 제공한다.
- 삭제 전에 취소될 일정·알림·연결 기기·백업 제외 범위를 쉬운 문장으로 보여주고 명시적 확인을 받는다.
- `모두 삭제`는 Windows 예약 알림과 mobile task를 먼저 폐기하고, 서버 삭제 확인 후 로컬 암호화 키와 DB를 제거한다. 네트워크 실패 시 서버 삭제 재시도용 최소 tombstone만 남긴다.
- SSD에서 물리적 덮어쓰기를 보장한다고 표현하지 않고 암호화 키 폐기로 복호화 불가능하게 만드는 cryptographic erase를 사용한다.
- 사용자가 별도로 내보낸 백업과 원본 XLS는 앱이 임의 삭제하지 않으며 위치와 수동 삭제 방법을 안내한다.
- 관리형 silent uninstall은 질문을 띄울 수 없으므로 학교 관리자 가이드에 사용자 데이터 보존·삭제 명령을 별도로 제공한다.

### 지원

- 앱 내 `문제 해결`에서 알림 테스트, DB 상태, 저장공간, 네트워크, 모바일 연결을 각각 검사한다.
- 오류코드 검색이 가능한 도움말 페이지를 제공한다.
- 원문 없는 진단 파일을 사용자가 직접 확인한 뒤 첨부할 수 있다.
- 개인정보·보안 신고 전용 연락처와 일반 문의 채널을 분리한다.
- 지원 종료 OS·앱 버전은 최소 6개월 전에 안내한다.

### 운영 비용 보호

- 모바일 Push와 Firestore 사용량에 사용자별·기기별 rate limit을 둔다.
- 동일 알림 재시도 횟수를 제한한다.
- 비정상 기기 token과 만료 subscription을 정기 삭제한다.
- 예산 한도와 사용량 알림을 설정한다.
- 서버 장애 시 푸시를 무한 재시도하지 않는다.

---

## 20B. 단계별 사용자 검증

전국 공개 전 다음 순서로 검증한다.

### 1단계 — 제작자 테스트

- 합성 XLS와 사용자가 명시적으로 허용한 개인정보 제거 실제 구조 파일
- Windows 10/11, 100~150% 배율
- 오류·중복·민감정보·변경·취소·반복·날짜 충돌 테스트

### 2단계 — 교사 5명

- 설명 없이 설치와 첫 가져오기 관찰
- 3분 내 첫 성공 여부
- 막힌 지점과 이해하지 못한 용어 기록

### 3단계 — 학교 3곳·교사 30명

- 서로 다른 PC 보안정책·쿨메신저 버전·업무 역할
- 2주간 오탐·누락·알림 피로도 측정
- 원문을 수집하지 않고 사용자 표시 결과에 대한 피드백만 수집

### 4단계 — 공개 베타 100명 이상

- 설치 성공률, 비정상 종료, 푸시 성공률, 후보 승인·무시율 점검
- 텔레메트리는 명시적 동의와 익명 집계만 허용

### 공개 기준

- 설치 성공률 95% 이상
- 첫 성공 3분 이내 달성 사용자 90% 이상
- 공식 일정 Precision 95% 이상
- 공식 일정 Recall 90% 이상. 단, 개인정보 안전 필터로 제외된 항목은 별도 보고
- 민감정보 탐지 Recall 99% 이상인 합성·비식별 golden set 통과
- 민감정보 노출 사고 0건
- “알림이 너무 많다” 응답 10% 미만
- 복구 불가능한 데이터 손실 0건
- 테스트 기기·정상 네트워크에서 모바일 푸시의 5분 이내 도착률 95% 이상. OS에 의한 지연·권한 해제는 별도 분류

---

## 21. 구현 우선순위와 중단점

### R0 — Claude Code가 지금 구현할 범위

1. 프로젝트 골격, SQLite migration, Windows 사용자별 경로
2. Windows 예약 알림 기술 스파이크: 설치 빌드·창 닫힘·프로세스 종료·재부팅·절전 복귀
3. Source Adapter와 합성 XLS/XLSX fixture
4. 인용문 분리, 파일 간 중복 제거, 민감정보 마스킹
5. 날짜·시간·다중 일정·변경·취소·반복 추출
6. 규칙 기반 분류기와 golden set
7. 일정 후보 확인함과 안전조건 자동등록 (9장 기준)
8. 캘린더·할 일·수정·취소·되돌리기
9. Windows 알림 스케줄러와 방해금지
10. R0 전체 테스트, README, EVALUATION, 위협 모델
11. **여기서 중단하고 R0 결과 승인을 요청한다.**

### 승인 후 순서

12. R1 위젯, 설치·서명, 진단, 백업·복원, 업데이트 롤백
13. R1 교사 5명 검증과 수정
14. R3 공급사 검토 후 쿨메신저 다운로드 자동화

### 향후 확장 순서 — 이번 구현 범위 제외

R2를 다시 추진하기로 별도 결정한 경우에만 아래 순서를 새 Release Gate로 시작한다. 현재 구현에서는
`mobile/`, `mobile-pwa/`, `firebase/` 디렉터리를 만들거나 모바일 서버·PWA를 배포하지 않는다.

1. R2 종단간 암호화 동기화, QR pairing, PWA, Web Push
2. R2 모바일 위협 모델·삭제·재시도·비용 테스트
3. R2 인수 테스트 결과 제출과 별도 승인
4. R4 학교 3곳·공개 베타·전국 배포 준비

---

## 22. 구현 시 금지사항

- 실제 제공된 쿨메신저 XLS를 저장소에 커밋하지 않는다.
- API 키·학교 계정·비밀번호를 코드에 넣지 않는다.
- 원문을 디버그 로그에 출력하지 않는다.
- 사용자 확인 없이 선택 행사를 등록하지 않는다.
- 날짜가 있다는 이유만으로 일정으로 분류하지 않는다.
- UI 자동화 실패 시 무한 클릭·무한 재시도를 하지 않는다.
- 쿨메신저 내부 DB를 직접 변경하지 않는다.
- 휴대폰 알림을 이유로 원본 메시지나 XLS를 서버에 업로드하지 않는다.
- 일정 제목·장소·메모를 모바일 서버 DB·로그·작업 큐에 평문으로 저장하지 않는다.
- 장기 유효 QR pairing token을 만들지 않는다.
- 암호화 키와 capability token을 같은 장기 token으로 사용하지 않는다.
- VAPID 비밀키나 Firebase 관리자 키를 클라이언트 코드에 포함하지 않는다.
- 회원가입을 해야만 로컬 일정 기능을 쓸 수 있게 만들지 않는다.
- 첫 실행에서 전체 메시지 기록을 기본 분석하지 않는다.
- 무서명 실행파일을 전국 사용자에게 배포하지 않는다.
- 업데이트 실패 시 사용자 DB를 초기화하지 않는다.
- 사용자가 동의하지 않은 사용 통계·오류 원문을 수집하지 않는다.
- 공급사 호환성·허용 여부를 검토하기 전에 쿨메신저 UI 자동조작을 전국 공개하지 않는다.
- 날짜·요일 불일치나 변경 대상 불명확을 임의로 결정하지 않는다.
- 단일 Windows 공용 계정의 데이터를 여러 교사에게 안전하다고 표현하지 않는다.
- 개인정보보호 전문가 검토 없이 `법적 준수 완료`, `완전 익명`, `절대 안전`이라고 홍보하지 않는다.

---

## 23. 외부 제약 참고자료

- UI 요소·패턴: https://namethatui.com/
- Windows 앱 배포 경로: https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/choose-distribution-path
- Windows 코드 서명: https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/code-signing-options
- Microsoft Store MSIX 인증: https://learn.microsoft.com/en-us/windows/apps/publish/publish-your-app/msix/app-certification-process
- Tauri Windows installer: https://v2.tauri.app/distribute/windows-installer/
- Tauri Microsoft Store 배포: https://v2.tauri.app/distribute/microsoft-store/
- Windows 예약 앱 알림: https://learn.microsoft.com/en-us/windows/apps/develop/notifications/app-notifications/scheduled-toast
- iPhone·iPad PWA Web Push: https://webkit.org/blog/13878/web-push-for-web-apps-on-ios-and-ipados/
- Firestore 위치: https://firebase.google.com/docs/firestore/locations
- Cloud Functions 위치: https://firebase.google.com/docs/functions/locations
- Firebase Task queue functions: https://firebase.google.com/docs/functions/task-functions
- 개인정보보호위원회 안전성 확보조치 안내서: https://www.pipc.go.kr/np/cop/bbs/selectBoardArticle.do?bbsId=BS217&mCode=D010030020&nttId=11641
- 개인정보보호위원회 처리방침 작성지침(2026.4.): https://www.pipc.go.kr/np/cop/bbs/selectBoardArticle.do?bbsId=BS217&mCode=G010030000&nttId=12018

---

## 24. 구현자가 임의 결정하면 안 되는 출시 항목

R0 구현을 시작하는 데 아래 결정은 필요하지 않다. 해당 Gate에 도달했을 때만 사용자에게 묻고 ADR로 남긴다.

| Gate | 사전 결정 또는 증적 |
|---|---|
| R1 | 실제 게시자명, 코드 서명 인증서·보관 방식, Store 개발자 계정, 직접 배포 도메인, 백업 recovery UX |
| R2 | 서비스 운영 주체, Firebase 프로젝트·결제·리전, 개인정보 처리 법적 근거와 방침, 위탁·국외 처리 검토, 삭제·보안 신고 담당자 |
| R3 | 쿨메신저 공급사 호환성·자동조작 허용 근거, 지원 버전, 상표·비제휴 표시, kill switch 운영자 |
| R4 | 공개 제품명, 지원 범위·응답 기준, 베타 동의문, 장애·보안 사고 대응자, 운영 예산 한도 |

Claude Code는 값을 가짜로 채우거나 개발용 비밀키를 운영용으로 재사용하지 않는다. 미결정 항목은 `OPEN_DECISIONS.md`에 소유자·필요 시점·기본 안전 동작과 함께 기록한다.
