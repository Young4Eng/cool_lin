# 대회 보안 체크리스트 대응 및 보완안

> 대상 제품: 교사 일정도우미  
> 기준 문서: 대회 제공 [보안 체크리스트](https://boan-hack-me.vercel.app/checklist)  
> 검토 대상: `PRD(1).md`, `교사 일정도우미 기술계획서.md`  
> 작성일: 2026-08-28

---

## 1. 문서 목적

이 문서는 대회에서 제공한 보안 체크리스트를 기준으로 교사 일정도우미의 설계 충족 여부를 확인하고, 현재 PRD와 기술계획서에 추가해야 할 보안 명세와 인수 테스트를 정리한 문서다.

대회 평가 시에는 기능의 개수보다 다음 세 가지를 분명하게 증명한다.

1. 적용되는 보안 항목은 설계와 테스트로 충족한다.
2. 적용되지 않는 항목은 제품 구조상 존재하지 않는 공격 표면임을 설명한다.
3. 프론트엔드를 우회해도 Rust backend에서 보안 검증이 다시 수행된다.

---

## 2. 평가 범위와 보안 전제

### 2.1 이번 대회 구현 범위

- 구현 대상: R0 로컬 파이프라인, R1 위젯, R3 쿨메신저 자동 다운로드
- 제외 대상: R2 모바일 PWA, Firebase 서버 동기화, Web Push
- 실행 환경: Windows 10/11의 사용자 계정 범위
- 저장 방식: 사용자별 로컬 SQLite
- 계정 체계: 앱 자체 회원가입·로그인 없음
- 외부 API: 없음
- AI·LLM 호출: 없음
- 원격 DB·공개 스토리지: 없음

### 2.2 `해당 없음`의 의미

로그인·웹서버·공개 API·쿠키 세션·스토리지 버킷을 구현하지 않았기 때문에 관련 취약점이 현재 제품 범위에 존재하지 않는다. 체크리스트를 맞추기 위해 불필요한 서버나 회원가입을 추가하지 않는다. 공격 표면을 만들지 않은 것 자체를 보안 설계로 설명한다.

앱의 `역할 태그`는 담임·담당 학년·보직에 따른 일정 관련성 점수 계산에만 사용한다. 관리자 권한이나 데이터 접근권한을 부여하지 않는다.

---

## 3. 대회 보안 체크리스트 대응표

판정 기준:

- **충족:** 현재 문서에 구현 규칙과 검증 기준이 모두 있음
- **부분 충족:** 설계 취지는 있으나 구현 금지 규칙 또는 테스트가 부족함
- **보완 필요:** 대회 기준에 직접 대응하는 명세가 없음
- **해당 없음:** 현재 구현 범위에 해당 공격 표면이 없음

| 영역 | 대회 체크 항목 | 판정 | 현재 근거 | 필요한 조치 |
|---|---|---:|---|---|
| 인증 | 로그인 확인을 서버가 수행한다 | 해당 없음 | 로컬 전용이며 앱 계정·로그인·인증 서버가 없음 | 대응표에 적용 제외 사유 유지 |
| 인가 | 관리자 등의 역할을 서버가 결정한다 | 해당 없음 | 역할 태그는 분류용 로컬 설정이며 권한이 아님 | 역할 태그가 인가 정보가 아님을 명시 |
| 인가 | DB/API가 요청자별로 데이터를 제한한다 | 해당 없음·대체 통제 | 원격 DB/API 없음. SQLite와 키를 Windows 사용자 프로필별로 분리 | 다른 Windows 계정에서 접근할 수 없는지 인수 테스트 유지 |
| 인가 | 다른 사람의 ID로 바꿔 요청해도 차단한다 | 해당 없음 | 외부 URL·API·사용자별 리소스 ID가 없음 | R2가 실제 범위에 포함될 때만 IDOR 테스트 적용 |
| 입력 불신 | 입력을 화면에 넣을 때 글자로 처리한다 | 충족 | XLS 값을 plain text로 취급하고 HTML·Markdown·`dangerouslySetInnerHTML` 사용을 금지 | `<script>`, `file://`, bidi 문자 테스트 유지 |
| 입력 불신 | DB 쿼리를 파라미터로 바인딩한다 | **보완 필요** | `rusqlite` 사용만 명시되어 있고 문자열 연결 금지 규칙이 없음 | prepared statement·파라미터 바인딩 규칙과 주입 테스트 추가 |
| 노출 | 비밀키·비밀번호가 코드와 프론트엔드에 없다 | 충족 | API 키·학교 계정·비밀번호 하드코딩 금지, 암호화 키는 DPAPI로 보호 | 릴리스 비밀값 탐지 검사를 추가 |
| 노출 | 관리자·마스터키가 브라우저에 없다 | 부분 충족 | 암호화 처리는 Rust backend에서 수행하도록 설계 | 복호화 키를 WebView IPC·event로 보내지 않는다고 명시 |
| 노출 | 배포물에 `.env`·`.sql`·백업 파일이 없다 | **보완 필요** | 설치·서명 기준은 있으나 배포 산출물 검사 기준이 없음 | 배포 denylist 검사와 SQL migration 내장 규칙 추가 |
| 파일·스토리지 | 스토리지 버킷을 public으로 방치하지 않는다 | 해당 없음 | 현재 범위에 클라우드 스토리지 없음 | R2 제외 사실을 명확히 표시 |
| 운영 | 프로덕션 debug 모드를 끈다 | **보완 필요** | 원문 debug 로그 금지는 있으나 DevTools·stack trace·source map 규칙이 없음 | release build hardening 규칙 추가 |
| 공급망 | 오래된 라이브러리와 알려진 취약점을 점검한다 | 부분 충족 | lockfile·라이선스 목록·SBOM·critical 취약점 차단이 명시됨 | `cargo audit`, `npm audit`, secret scan과 차단 기준 추가 |
| 남용 방지 | 로그인·API에 rate limit을 적용한다 | 해당 없음 | 로그인과 외부 API 없음. UI 자동화는 단계별 timeout·재시도 제한 적용 | 현재 범위에는 적용 제외, R2 재개 시 필수 적용 |
| 입력 불신 | 클라이언트 검증 후 backend에서 다시 검증한다 | **보완 필요** | Rust에서 파일을 처리하지만 프론트 검증을 신뢰하지 않는다는 규칙이 없음 | 모든 Tauri command의 Rust 재검증 규칙과 우회 테스트 추가 |
| 비밀번호 | 비밀번호를 안전한 전용 해시로 저장한다 | 해당 없음 | 앱 로그인 비밀번호 없음. 백업 복구 암호는 Argon2id 사용 | 로그인 비밀번호와 백업 암호를 구분해 설명 |
| 세션 | CSRF를 방어한다 | 해당 없음 | HTTP 쿠키 인증·웹서버 없음 | 로컬 asset만 로드하고 원격 WebView에 capability를 주지 않는 대체 통제 유지 |

---

## 4. PRD·기술계획서에 반드시 추가할 보안 명세

다음 내용은 PRD의 `개인정보·보안 요구사항`, 기술계획서의 `저장소 설계·테스트·배포` 절에 반영한다.

### 4.1 SQLite 파라미터 바인딩

**MUST**

모든 SQLite 쿼리는 `rusqlite` prepared statement와 파라미터 바인딩을 사용한다. 사용자 입력, XLS 셀 값, 파일명, 검색어, 일정 제목을 SQL 문자열에 직접 연결하지 않는다. 동적 테이블명·열 이름이 필요한 경우 사전에 정의된 allowlist에서만 선택한다.

금지 예시:

```rust
let sql = format!("SELECT * FROM messages WHERE title = '{}'", user_input);
```

허용 예시:

```rust
let mut stmt = conn.prepare("SELECT * FROM messages WHERE title = ?1")?;
let rows = stmt.query([user_input])?;
```

### 4.2 프론트엔드 검증과 Rust backend 재검증

**MUST**

React의 파일 확장자, 입력 길이, 날짜 형식 검사는 사용자 안내와 빠른 오류 표시를 위한 UX 검증으로만 사용한다. 모든 Tauri command는 호출될 때마다 Rust에서 다음 항목을 독립적으로 재검증한다.

- 요청한 파일 경로가 허용된 scope 안에 있는지
- 확장자가 `.xls` 또는 `.xlsx`인지
- 확장자와 실제 file signature가 일치하는지
- 파일 크기·시트 수·행 수·셀 길이가 안전 한도 안인지
- 필수 시트와 헤더 구조가 맞는지
- 날짜·상태·알림 시각 등의 값이 허용 범위 안인지
- 사용자 입력이 파일 경로·SQL·명령 실행 인자로 사용되지 않는지

프론트엔드를 우회하여 Tauri command를 직접 호출하더라도 잘못된 요청은 Rust에서 거부한다.

### 4.3 암호화 키의 프론트엔드 격리

**MUST**

복호화된 content key, fingerprint key, DPAPI로 해제된 키 자료는 다음 위치에 포함하지 않는다.

- React state와 브라우저 저장소
- WebView IPC 응답
- Tauri event payload
- clipboard
- 일반 로그·진단 보고서·crash report

암호화·복호화는 Rust backend에서만 수행한다. 프론트엔드에는 마스킹된 DTO와 화면 표시에 필요한 최소 데이터만 전달한다.

### 4.4 프로덕션 빌드 보안 설정

**MUST**

공개 설치파일은 release mode로만 빌드한다.

- 운영 빌드에서 WebView DevTools를 비활성화한다.
- debug 로그와 상세 stack trace를 사용자 화면에 표시하지 않는다.
- source map을 공개 설치파일과 배포 ZIP에 포함하지 않는다.
- Rust panic·SQL 오류·내부 파일 경로 대신 일반 오류코드를 표시한다.
- 오류코드와 개인정보가 제거된 최소 진단 정보만 기록한다.
- 개발 빌드와 운영 빌드의 앱 이름·아이콘·설정·데이터 경로를 구분한다.

### 4.5 배포 산출물 검사

**MUST**

최종 MSI·EXE·배포 ZIP에는 빌드 실행에 필요한 파일만 포함한다. 배포 직전에 자동 검사하여 다음 파일이 하나라도 발견되면 릴리스를 중단한다.

- `.env`, `.env.*`
- 개발용 API 키·토큰·계정정보
- `.db`, `.sqlite`, `.bak`, 백업파일
- 실제 또는 fixture XLS/XLSX
- 원문 메시지·golden set 원본
- debug log·crash dump
- source map
- 개발 스크립트와 임시파일
- 비밀값이나 실제 데이터가 포함된 `.sql`

SQL migration 원본은 소스 저장소에서 관리하되 배포 폴더에 그대로 노출하지 않고 바이너리에 포함한다. 스키마 파일을 별도로 포함해야 한다면 데이터와 비밀값이 없는지 검사한다.

### 4.6 의존성·비밀값 검사

**MUST**

CI와 릴리스 직전에 다음 검사를 수행한다.

- Rust 의존성: `cargo audit`
- JavaScript 의존성: `npm audit`
- 하드코딩 비밀값: secret scan
- lockfile 변경 여부 확인
- SBOM과 라이선스 목록 생성

해결되지 않은 high·critical 취약점이나 실제 비밀값이 탐지되면 공개 배포를 중단한다. 불가피하게 보류하는 취약점은 영향 범위, 임시 완화책, 담당자, 해결 기한을 `OPEN_DECISIONS.md`에 기록한다.

---

## 5. 대회 체크리스트 기준 추가 인수 테스트

### 5.1 SQL 주입 방지

- [ ] 제목·검색어에 `'`, `"`, `;`, `--`가 포함되어도 정상적인 값으로 저장·검색된다.
- [ ] SQL 구문 형태의 문자열을 입력해도 쿼리 구조가 바뀌지 않는다.
- [ ] DB 오류·테이블명·쿼리 원문이 사용자 화면과 로그에 노출되지 않는다.
- [ ] 코드 검색 결과 사용자 입력을 SQL에 연결하는 `format!` 또는 문자열 조합이 없다.

### 5.2 backend 재검증

- [ ] 프론트엔드를 거치지 않고 잘못된 확장자의 파일 경로로 Tauri command를 호출해도 거부한다.
- [ ] 확장자는 `.xls`지만 file signature가 다른 파일을 거부한다.
- [ ] 허용 경로 밖 파일과 `..` 경로를 거부한다.
- [ ] 크기·행 수·셀 길이 한도를 넘는 파일을 부분 저장 없이 거부한다.
- [ ] 실패한 import가 후보·일정·알림을 반쪽 상태로 남기지 않는다.

### 5.3 프론트엔드 노출 방지

- [ ] React 개발자도구·IPC 응답·Tauri event에 복호화 키가 나타나지 않는다.
- [ ] `<script>`, HTML, Markdown, `file://`가 포함된 XLS 셀을 글자로만 표시한다.
- [ ] 원문 XLS와 마스킹 전 메시지가 React state·clipboard·로그에 남지 않는다.

### 5.4 운영 빌드

- [ ] 실제 release installer에서 WebView DevTools를 열 수 없다.
- [ ] 의도적으로 오류를 발생시켜도 stack trace·내부 경로·SQL이 표시되지 않는다.
- [ ] 설치파일과 배포 ZIP에 source map과 debug log가 없다.
- [ ] 개발 빌드 실행이 운영 앱의 자동실행 경로와 운영 DB를 덮어쓰지 않는다.

### 5.5 배포 산출물

- [ ] 배포물에 `.env`, DB, 백업, XLS, fixture, 실제 원문, source map이 없다.
- [ ] 저장소와 Git history에 실제 쿨메신저 XLS·계정·API 키가 없다.
- [ ] 설치파일의 SHA-256이 배포 안내에 적힌 값과 일치한다.
- [ ] 코드 서명 검증 결과 게시자와 파일 무결성이 확인된다.

### 5.6 의존성·비밀값

- [ ] `cargo audit` 결과 해결되지 않은 high·critical 취약점이 없다.
- [ ] `npm audit` 결과 해결되지 않은 high·critical 취약점이 없다.
- [ ] secret scan 결과 실제 비밀값이 없다.
- [ ] `Cargo.lock`과 npm lockfile이 저장소에 포함되어 있다.
- [ ] 릴리스 버전에 대응하는 SBOM을 생성했다.

---

## 6. 대회 제출 전 보안 Release Gate

다음 항목을 모두 통과하기 전에는 `보안 검증 완료`라고 표현하지 않는다.

| Gate | 통과 조건 | 결과 |
|---|---|---|
| G1 공격 표면 확인 | 서버·계정·외부 API·클라우드 저장소가 실제 빌드에 포함되지 않음 | [ ] |
| G2 입력 불신 | 파일 시그니처·안전 한도·Rust 재검증·XSS 방지 테스트 통과 | [ ] |
| G3 저장 안전 | 원문 미저장·마스킹·Windows 사용자 분리·키 격리 테스트 통과 | [ ] |
| G4 DB 안전 | 모든 SQLite 쿼리 파라미터 바인딩 및 주입 테스트 통과 | [ ] |
| G5 운영 빌드 | release mode·DevTools OFF·상세 오류 비노출 | [ ] |
| G6 배포물 청결 | 비밀값·DB·백업·XLS·source map이 산출물에 없음 | [ ] |
| G7 공급망 | audit·secret scan·SBOM·코드 서명 기준 통과 | [ ] |

Gate 하나라도 실패하면 상태는 `수정 후 재검증`이다.

---

## 7. 심사위원 설명용 핵심 문구

### 7.1 20초 설명

> 대회 보안 체크리스트를 기준으로 모든 항목을 적용 여부부터 검토했습니다. 이 앱은 회원가입과 외부 서버 없이 교사 PC에서만 동작하므로 인증·IDOR·CSRF 같은 웹 공격 표면을 만들지 않았습니다. 대신 실제 위험인 XLS 입력, SQLite 쿼리, 개인정보 마스킹, WebView 권한, 배포파일 노출을 Rust backend와 인수 테스트로 통제했습니다.

### 7.2 역할 태그 관련 질문

> 역할 태그는 관리자 권한이 아닙니다. 담임·담당 학년에 따라 어떤 공지가 사용자에게 관련 있는지 계산하는 로컬 분류 설정이며, 데이터 접근권한을 부여하거나 해제하지 않습니다.

### 7.3 서버 인증이 없다는 질문

> 앱 핵심 기능은 개인 Windows 계정 안에서 동작하는 단일 사용자 로컬 프로그램입니다. 불필요한 회원가입과 서버를 추가하면 개인정보 전송과 계정 공격 표면이 새로 생기므로, 이번 범위에서는 서버 자체를 두지 않는 구조를 선택했습니다.

### 7.4 클라이언트 우회 질문

> React 검증은 편의를 위한 1차 안내일 뿐입니다. 파일 경로·시그니처·크기·헤더·SQL 값은 Tauri command가 호출될 때마다 Rust에서 다시 검증하므로 프론트엔드를 우회해도 처리되지 않습니다.

---

## 8. 최종 판정

현재 PRD와 기술계획서는 개인정보 최소화, 원문 미저장, 로컬 우선, 최소권한, 파일 파싱 안전성 측면에서 대회 체크리스트보다 구체적인 설계를 포함하고 있다.

다만 다음 네 항목은 문서와 구현에 반드시 추가해야 한다.

1. SQLite prepared statement와 파라미터 바인딩
2. 모든 Tauri command의 Rust backend 재검증
3. 운영 빌드의 DevTools·stack trace·source map 차단
4. 배포 산출물의 `.env`·DB·백업·XLS·비밀값 자동 검사

암호화 키의 IPC 격리와 의존성 검사 명령까지 명시하면 대회 체크리스트의 현재 구현 범위에 해당하는 항목을 모두 설명할 수 있다.
